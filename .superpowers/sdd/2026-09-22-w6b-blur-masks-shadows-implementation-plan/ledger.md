# SDD ledger — plan: refactor/plans/2026-09-22-w6b-blur-masks-shadows-implementation-plan.md

## Setup

- Workspace: `/Users/chaos/.codex/worktrees/cbf6/kanvas`
- Branch: `codex/w6b-blur-masks-shadows`
- Base before execution: `1ff67ec849e66de4c1ac2767ddb1dffce8d40631`
- Plan: six sequential tasks; Terra implements, Sol reviews.
- Constraints: public behavioral tests only; no infrastructure/private/reflection/mock/fake-device/counter/static-source tests; geometry stays in `:math` with I32/I64/F32/F64 names; fonts, codecs, GMs, dashboard, renders, baselines, scores, `jpg-color-cube`, and global Skia are excluded.

## Preflight scan

| Scope | Shared or self-consistency concern | Resolution |
| --- | --- | --- |
| T1 | `render-ir` cannot import public `DropShadowMode` from `:kanvas` without a dependency cycle. | R1 |
| T2 | Recovery test used `ColorARGB.Blue` but expected `(17,61,211)`. | R3 |
| T3 | Oracle file and snippet used different class names. | R4 |
| T4 | Auto-layer ownership and ordering are internally consistent. | No change. |
| T5 | `MaskShader` snippet passed a nonexistent mapping field; captured table uses `sizeI32`. | R5, R6 |
| T6 | Public mode semantics, budget, and convergence are internally consistent after R1. | No change. |
| T1→T2 | Table roots and captured mode are the admission inputs. | R1; T2 consumes captured types only. |
| T1→T3 | Picture identity test did not directly prove shared-vs-equal-distinct objects. | R2; Task 3 adds pixel replay proof. |
| T1→T4 | Captured roots must remain stable through auto-layer construction. | Existing typed-root contract retained. |
| T1→T5 | Historical decode and table validation need distinct phases. | Decode remains permissive; admission owns length refusal. |
| T1→T6 | Public and captured shadow modes cross only API adapters. | R1. |
| T2→T3 | Blur must reuse the frozen graph, targets, bounds, and budget. | Existing Task 2 contract retained. |
| T2→T4 | Auto-layers extend the same graph rather than forming another authority. | Existing Task 4 contract retained. |
| T2→T5 | Material/table arms use Task 2 evaluation keys and budget. | R5, R6. |
| T2→T6 | Shadow passes and atomicity use the same graph/budget. | Existing Task 6 contract retained. |
| T3→T4 | Coverage blur reuses typed separable passes, not a renderer planner. | Existing interface retained. |
| T3→T5 | Picture replay/materialization remains single-authority. | Existing interface retained. |
| T3→T6 | Shadow blur reuses Task 3 passes and frame lifetime. | Existing interface retained. |
| T4→T5 | Mask shader/table extend Task 4 auto-layer coverage before W5 shading. | Existing order retained. |
| T4→T6 | Drop shadow may consume auto-layer source but keeps one final blend. | Existing order retained. |
| T5→T6 | Table/material resources stay in the global peak budget through completion. | Existing Task 6 audit retained. |

## Rulings

- **R1 — public/captured mode split:** `DropShadowMode` remains a public `:kanvas` API type. `render-ir` owns `CapturedDropShadowModeV1`; API adapters map exhaustively in both directions. `gpu-plan` and `gpu-renderer` carry only captured or plan-owned modes. Historical schemas default to captured `COMPOSITE`. Cost if wrong: an impossible module dependency cycle or a second public semantic authority.
- **R2 — public identity proof:** Task 1 must use public Picture operation traversal/playback to assert that the first two shared filters are reference-identical after memory and wire replay while the equal-but-distinct third filter is not. Bytes still round-trip exactly. Cost if wrong: value deduplication could silently violate capture identity.
- **R3 — recovery color:** the Task 2 recovery draw uses `ColorARGB.of(255, 17, 61, 211)` to match the expected bytes. Cost if wrong: a false RED unrelated to admission recovery.
- **R4 — oracle name:** Task 3 uses `W6bImageBlurCpuOracle`, matching the planned file. Cost if wrong: compilation-only failure instead of a behavioral RED.
- **R5 — mapping ownership:** `FilterPassOperationV1.MaskShader` has no mapping field; the materializer obtains the sealed mapping from the surrounding `FilterEvaluationKeyV1`. Cost if wrong: duplicated or contradictory coordinate authority.
- **R6 — immutable table size:** planned `ImmutableUBytes` validation uses `sizeI32`; public `UByteArray` capture may use `size`. Cost if wrong: compilation failure or validation at the wrong boundary.

## Task status

- [x] Task 1 — immutable filter table, shadow API, Picture 14/schema 8
- [x] Task 1 Sol review and bounded correction if needed
- [x] Task 2 — W6b authority, bounds, targets, admission
- [x] Task 2 Sol review and bounded correction if needed
- [x] Task 3 — image blur and tile modes
- [x] Task 3 Sol review and bounded correction if needed
- [x] Task 4 — mask blur auto-layers
- [x] Task 4 Sol review and bounded correction if needed
- [x] Task 5 — mask shader/table
- [x] Task 5 Sol review and bounded correction if needed
- [x] Task 6 — shadows, budget, convergence
- [x] Task 6 Sol review and bounded correction if needed
- [x] Whole-branch Sol review and bounded correction if needed
- [x] Verification ciblée de la correction (sans push, PR ni artifact dans ce périmètre)

## Execution log

- Preflight completed before Task 1 dispatch. Plan amendments encode R1–R6.
- Plan amendment committed as `9add300`; Task 1 implementation base is `9add300`.
- Task 1 initial implementation committed as `1b867d53e`; W6b test/compile evidence passed, two regression processes ended 133 after passing JUnit and remain `UNKNOWN`.
- Task 1 Sol review: `CHANGES_REQUESTED` (one critical missing typed table; two important findings covering mutable snapshot isolation and historical/mode proof). One bounded correction wave authorized by the plan.
- Task 1: fix round 1/5 (3 original findings addressed, 2 new Important regressions open — standalone paint root ownership; stale Render IR test source; commits `1b867d5..c1e36dd`).
- **R7 — correction-cap exception:** the approved per-task one-wave cap cannot leave new corrected-surface regressions that make a public adapter lossy and the owning module's test source uncompilable. Permit one narrowly scoped second correction and one scoped re-review, with no reopening of the three addressed findings. Cost if wrong: one extra Terra/Sol round beyond the plan's quota guard; refusing the exception would carry known breakage into every dependent task.
- Task 1 fix round 2 implemented as `7ddbaa556`: standalone capture now returns an owning `CapturedPaintV1`; the stale codec fixture uses a table-owned typed root. Public adapter 1/1, W6b 7/7, mutable snapshot 1/1, and production compiles pass. `:render-ir:compileTestKotlin` reaches only the pre-existing `MaterialNodeTest.kt:132` inference failure.
- Task 1: fix round 2/5 (2 addressed, 0 open; commits `c1e36dd..7ddbaa5`). Scoped Sol re-review: `PASS`.
- Task 1 deferred pre-existing risk: `:render-ir:compileTestKotlin` has an unrelated `MaterialNodeTest.kt:132` type-inference failure not introduced by W6b Task 1; production compiles and all Task 1 public selectors are green.
- Task 2 brief regenerated after recovery-color plan correction `ee84ea48d`; Task 2 implementation base is `ee84ea48d`.
- Task 2 initial implementation committed as `de40ebdbc`. Sol review: `CHANGES_REQUESTED` — one Critical terminal-capture fallback and four Important gaps (no real frozen W6b occurrences; blur X/Y pass contract conflict; wrong target-local origin; nested Picture ownership/public diagnostic proof). The five reported `gpu-plan` test-source compile errors are blob-identical at Task 2 base/head and not attributable to this task.
- Task 2 fix round 1 implemented as `d7b8ffb9d`: closes terminal fallback, constructs real W6b graph resources/passes/bounds/budget, permits blur X/Y shared keys, uses explicit target origins, traverses nested Pictures/EffectStack, and expands public terminal/recovery proof. Production compiles pass; W6b 6/6, W6a pixels 16/16, bounds 10/10 are green in JUnit before native exit 133 (`UNKNOWN`).
- Task 2: fix round 1/5 (3 original findings addressed, 2 original Important findings open, 1 new Important breakage — occurrence graph remains semantically disconnected; graph-level X/Y witness missing; frame-global Picture lane suppression; commit `de40ebd..d7b8ffb`).
- **R8 — Task 2 second correction:** the remaining occurrence/source/order and graph-witness defects are load-bearing for Tasks 3–6, and the new Picture-lane suppression is a real regression. Permit a second scoped correction despite the plan's quota cap; require truthful per-occurrence source generations and composites without implementing native pixels. Cost if wrong: another Terra/Sol round beyond the quota guard and possible premature encroachment on Task 3/4; not correcting would force downstream tasks to re-plan the graph and violate the single authority.
- Task 2 fix round 2 implemented as `265972efa`: immutable per-occurrence FilterSource/transparent/Picture sources, chronological filter chain and FilterComposite, publication witness, real bounds/style/shadow semantics, deferred mask material binding, and preserved Picture siblings. Production compiles pass; W6a 16/16 and W6b 8/8 are green in JUnit before native exit 133 (`UNKNOWN`).
- Task 2: fix round 2/5 (0 of 3 scoped findings fully closed, 4 Important open — mask/material/four-region graph still not truthful; witness accepts malformed chains; Picture lane remains placeholder/suppressed; W4e physical target mismatches semantic FilterSource; commit `d7b8ffb..265972e`).
- **R9 — Task 2 round-3 boundary:** Task 2 must freeze truthful semantic inputs without native execution. This includes separate raw coverage and shaded-source identities, real layer regions, exact W5 material ownership, and exact Picture occurrence metadata; Tasks 3–6 may materialize/extend typed operations but may not repair source identity or ordering. Cost if wrong: Task 2 grows into later-task scaffolding; the alternative leaves fabricated contracts that cannot be materialized safely.
- Task 2 fix round 3 implemented as final commit `820e76c`: coverage→W5 material→image ordering, real W5 mask material lanes, exact occurrence witness, exact Picture sources/nesting, W4e semantic/native target equality, and real layer regions. Production compiles pass; W4e 1/1, W6b Surface 8/8 and Picture 7/7 are green in JUnit; native exit 133 remains `UNKNOWN`.
- Task 2: fix round 3/5 (1 of 4 scoped findings addressed, 3 Important open — layer/stencil/expanded mask semantics; Picture coverage/shadow predecessor witness; mixed/nested Picture ordered publication; commit `265972e..820e76c`). Per SDD breaker policy, round 4 moves to a fresh higher-effort Terra implementer.
- **R10 — nested Picture stream contract:** after the independent Astra architecture review, `PictureStreamAggregateV1` has two frozen modes: an unpainted `drawPicture` executes `INLINE_CURRENT_TARGET`; any painted Picture uses `ISOLATED_SOURCE` with a versioned seal and one terminal composite. Task 2 now owns exhaustive `SceneCommand` partitioning, occurrence-local IDs, inner/deferred clips, cull versus reverse demand, graph-texture coverage/material semantics, exact source generations, lifetime validation and `w6b.picture_stream.invalid`. Tasks 3–6 materialize this frozen graph and supply the assigned positive Picture/mask/shadow proofs; they do not re-plan it. Cost if wrong: destination-dependent blends and `initWithPrevious` would observe a fabricated transparent target, parent filters could lose required samples or halos, source alpha could be applied twice, and later tasks would again become competing planners.
- Astra reviewed the amendment only and required six bounded corrections before resuming Task 2: inline versus isolated execution, clip/cull/demand coordinate stages, parent coverage/material semantics, exhaustive command vocabulary with occurrence identities, begin/seal/composite/lifetime state, and Task 2–6 ownership/proof gates. The specification and plan were amended before the paused implementation is resumed; this does not accept the uncommitted round-4 Kotlin changes.
- Task 2 fix round 4 implemented as `e25e2ea977`: post-mask W5 sources, typed stencil coverage, real nested W6a scopes, ordered Picture aggregates, inline/isolated modes, begin/seal generations, graph-texture operands, occurrence IDs and production validation. Production compilation and W4e pass; W6b 15/15 and W6a 16/16 pass in JUnit before native exit 133 (`UNKNOWN`); `:gpu-plan:compileTestKotlin` remains blocked only by the five established W3/W4d errors.
- Task 2: fix round 4/5 (3 of 9 reviewed findings addressed, 7 open — nested draws still lack frozen W4/W5 bindings; inline execution inherits that gap; enclosing transforms are not frozen for captured clips/demand; parent mask coverage is not bound to sealed aggregate alpha and paint-once remains underdetermined; repeated occurrences lack exact planned-source binding; terminal Picture composite lacks complete physical operands; nested `initWithPrevious` layer target lacks `CopySource`; commits `820e76c..e25e2ea`). Round 5 moves to a fresh Astra implementer because these are load-bearing handoff defects and the SDD fix-loop requires a capability escalation after round 4.
- **R11 — occurrence-local W4/W5 entry:** Task 2 adds one plan-owned `OccurrenceSourceInputV1` entry carrying the frame planned command ID, F64 occurrence mapping, target-local domains and distinct inner/deferred clips, then delegates to the existing W4 selectors and `MaterialSourceConstructionV4`. The top-level path and Picture path share the same source-lane/material authority; no renderer planner, public API or wire field is added. Cost if wrong: nested draws either inherit F32/fused-clip coordinates from the captured scene or create a competing W5 authority; the chosen adapter also has a known O(index) per-occurrence CPU construction cost and can be quadratic on very large frames until a later performance-only optimization.
- Task 2 fix round 5 implemented as `d8f85a49d`: occurrence-local W4/W5 draws, inline destination-read bindings, F64 clip/cull/demand separation, sealed-alpha parent coverage, exact occurrence/source IDs, complete Picture composite operands, and nested layer `CopySource`/single-restore semantics. Production compiles pass; W6b 19/19 and W6a 16/16 pass in JUnit before native exit 133 (`UNKNOWN`); W4e passes; contract execution remains blocked only by the five established W3/W4d compile errors.
- Task 2: fix round 5/5 (all 7 scoped findings addressed and all 3 preserved invariants retained; 1 new Important finding remains — frames containing a Picture stream disable the root source-order assertion without replacing root `draw A → Picture P → draw B` placement proof; commits `e25e2ea..d8f85a4`).
- **R12 — root/aggregate order at the breaker:** the remaining source-order validation gap is real and load-bearing, but its repair is bounded: partition root scene commands from synthetic occurrence-local commands, prove root pass/aggregate-terminal placement by the root source command order, and retain the existing per-aggregate internal order proof. Carry this as the mandatory first RED/GREEN of Task 3 before native materialization; do not reopen Task 2 with a sixth fix round. Cost if wrong: a valid root graph could publish `draw B` before the preceding Picture terminal even while every aggregate is internally ordered.
- Task 2: complete at the five-round breaker (commits `ee84ea4..d8f85a4`, 1 load-bearing validation finding carried to Task 3 under R12; no parked semantic/materialization gap). Scoped Sol rereview confirms all seven round-4 findings addressed and the three prior contracts preserved.
- **R13 — parent Picture W5 effects binding:** Task 3 may correct the Task-2 plan operand so `GraphTextureSourceOperandV1` references the actual parent W5 material/effects row and its frozen uniform resource, offset and capacity. `MaterialSourceConstructionV4`/`FrameSourceLayoutV4` remain the sole authority; the RGBA carrier stays neutral and alpha/color-filter/blend execute once. Cost if wrong: the renderer would synthesize missing uniforms or parent alpha/color filtering would be omitted or applied twice.
- **R14 — aggregate known content:** `PictureStreamAggregate.knownContent` is the union of child `SourceBinding.producedOutputDeviceI32` regions mapped into the target, never physical texture bounds, demand, cull or a transparent halo. Filter X/Y bounds are regenerated from this value while desired/required/produced regions remain distinct. Cost if wrong: CLAMP can replicate transparent halo texels and attenuate an otherwise opaque painted child.
- **R15 — frozen nested execution schedule:** the frame plan owns one total `frozenPassSchedule`, and each Picture aggregate owns its contiguous `executionPassIds`; child X→Y→terminal/composite completes before parent seal, then parent X→Y→terminal. The lowerer consumes this schedule without traversing or reconstructing hierarchy. Cost if wrong: nested filtered Pictures can execute parent filtering before child completion or be reordered by backend-local traversal.
- Task 3 initial implementation committed as `375255fec`: R12 root order, W6b native image blur X/Y, four tile modes, parent W5 binding, corrected aggregate known content, frozen nested schedule, public CPU-oracle pixels and memory/wire replay. `RenderGraphContractTest` is 94/94, production compiles and `:kanvas:test` pass; targeted native processes can exit 133 after green JUnit and remain `UNKNOWN`. Full `:gpu-plan:test` now executes and exposes 28 pre-task W3/W4 expectation/readback failures, documented but not changed by Task 3.
- Task 3 Sol review: `NEEDS_CHANGES` with five Important findings — admission re-traverses captured Scene/filter IR; MIRROR conflicts with the W5e `2 * dimension` texel contract and the oracle masks it; filtered Layer/Picture terminals omit frozen color-filter/destination-read execution; deferred DeviceRect clips are admitted more broadly than the scissor materializer supports; replay lacks a causal distinct source-generation graph assertion.
- Task 3: minor (deferred): rename new WGSL geometry/origin emitter parameters with explicit `I32` suffixes. The final branch review must triage this nomenclature finding.
- Task 3 fix round 1 implemented as `39491652d`: native admission now consumes only the frozen graph; MIRROR follows W5e with a discriminating asymmetric oracle; filtered Layer/Picture terminals consume frozen effects/snapshots; deferred clips are narrowed; repeated public captures prove distinct sealed source identities/generations. Targeted graph contracts and public shards pass; native 133 exits remain `UNKNOWN`; the 28 disclosed W3/W4 full-module failures remain pre-task debt.
- Task 3: fix round 1/5 (3 addressed, 2 open — an unfiltered painted child `PictureComposite` in a W6b graph still loses operand alpha/color-filter and destination-read snapshot; transformed-empty clips refuse and fractional hard-edge axis-aligned clips are over-admitted then rounded outward; commits `375255f..3949165`).
- Task 3 fix round 2 implemented as `237840d26`: unfiltered painted-child `PictureComposite` now applies frozen parent alpha/color-filter and destination snapshot exactly once; fractional hard-edge clips are refused exactly; transformed-empty and recovery fixtures were added. Targeted public shards and 94 graph contracts pass; native 133 remains `UNKNOWN`.
- Task 3: fix round 2/5 (1 addressed, 1 open — terminally empty aggregates suppress only ordinary Draw lane construction, while filtered/nested/layer/Clear/DrawColor entries can still plan work and a finite singular mapping still refuses before the no-op terminal; commits `3949165..237840d`).
- Task 3 fix round 3 implemented as `5df8c5228`: a terminally empty aggregate publishes a coherent begin→seal/no-op terminal with no visual entry lowering, graph-texture consumer, cull projection or mapping inversion, including a filtered child under a finite singular transform. Admission 25/25, image blur 10/10, Picture 9/9 and graph contracts 94/94 pass in JUnit; native 133 remains `UNKNOWN`.
- Task 3: fix round 3/5 (1 addressed, 0 open; commits `237840d..5df8c52`). Scoped Sol rereview: `PASS / READY`.
- Task 3: complete (commits `9d4c351..5df8c52`, review clean; one deferred Minor retained for final review).
- Task 4 initial implementation committed as `8d3899f9d`: native mask coverage blur X/Y and all four styles, explicit auto-layer parenting, one final blend, Picture parent/descendant masks, mask→image chaining, public CPU-oracle pixels and Task 3/W6a regression coverage. Production compiles pass; W6b mask 5/5, admission 25/25, Task 3 nested Picture 1/1, W6a layer 16/16 and restore 9/9 pass in JUnit before native exit 133 (`UNKNOWN`). The W6aNested command-limit test remains the same 9/10 base failure with the same diagnostic.
- Task 4 Sol review: `REQUEST_CHANGES` with two Important findings — native admission accepts analytic/stencil/non-rect W4 coverage while the materializer rediscovers and requires `SolidRect`; and stencil-cover auto-layer sources retain the selected blend before the same blend is applied again at the final composite.
- Task 4 fix round 1 implemented as `0e85abda1`: explicit `W6bRasterCoverageBindingV1` replaces renderer pass scanning, RRect/clip and stencil path coverage are planned resources, and every offscreen W6b source stage uses `SRC_OVER` before the selected final blend. Task 4 7/7, admission 25/25, graph contracts and Task 3/W6a controls pass in JUnit; native 133 remains `UNKNOWN`.
- Task 4: fix round 1/5 (1 original finding addressed, 1 partially addressed, 2 Important gaps open — direct-triangle paths without depth-stencil still receive a stencil-cover packet; path/point W5 material remains clipped to original geometry instead of covering the styled mask domain, producing black color halos; commits `8d3899f..0e85abd`).
- Task 4 fix round 2 implemented as `0a0359cc3`: direct paths use `Shading`/`FullOrScissor`, stencil retains its two-packet coverage path, and W5 material for point/direct/stencil evaluates across the full styled-coverage extent while raw W4 coverage remains separate. Task 4 9/9, admission 25/25, graph contracts 94/94 and 54 W6a controls pass in JUnit; native 133 remains `UNKNOWN`.
- Task 4: fix round 2/5 (2 addressed, 0 open; commits `0e85abd..0a0359c`). Scoped Sol rereview: `PASS / READY`.
- Task 4: complete (commits `3fde179..0a0359c`, review clean).
- **R16 — mask table resource and shader projection:** Task 5 repairs the missing Task-2 operand by publishing an immutable, occurrence-owned 256-byte mask-table resource with `entryCountI32`, generation, physical inventory, lifetime and checked I64 budget; invalid historical lengths refuse before resource publication. `MaskShader` receives a plan-owned projection of the existing W5 row, uniform resource/offset/capacity and `LayerMappingF64`; the renderer never accesses an internal W5 coordinate type or creates a second row/compiler/lane. Cost if wrong: table bytes escape admission/budget, shader coordinates diverge from capture, or the renderer becomes a late material planner.
- Task 5 initial implementation committed as `e729c88f5`: R16 immutable LUT resource, frozen W5 shader binding, explicit W4 coverage before mask alpha, parent/descendant Picture masks, memory/wire replay, mutation isolation, and historical format-13 invalid-length recovery. Production compiles pass; Task 5 7/7 and Picture 10/10 pass in JUnit before native exit 133 (`UNKNOWN`).
- Task 5 Sol review: `REQUEST_CHANGES` with three findings — direct Shader/Table sources retain destination-sensitive blend in the graph and rely on a backend override; native MaskShader binding rejects resource-bearing W5 rows that planning admits; repeated canonical MaskShader rows allocate duplicate native uniform buffers despite one physical resource. Report diagnostic spelling must also be corrected to `invalid.mask_filter.table_length`.
- Task 5 fix round 1 implemented as `32f573eb9`: all direct mask sources freeze `SRC_OVER`, full W5 manifests bind uniforms/storage/textures/samplers, native allocations are grouped by `PlanResourceId`, and public image-backed/repeated/non-SrcOver witnesses pass. Task 5 11/11, Picture 10/10 and graph contracts pass in JUnit; native 133 remains `UNKNOWN`.
- Task 5: fix round 1/5 (2 addressed, 1 partially addressed, 1 Important gap open — a resource-backed `MaskShader` on a synthetic saveLayer carrier still lacks the plan-owned white FILL/SRC_OVER paint required to publish its W5 manifest; commits `e729c88..32f573e`).
- Task 5 fix round 2 implemented as `2d683b286`: synthetic saveLayer carriers receive the neutral plan-owned white FILL/SRC_OVER paint, publish the full resource-backed W5 manifest, and render an in-memory image MaskShader. Admission 25/25, Task 5 12/12, Picture 10/10 and graph contracts pass in JUnit; fractional deferred clips retain the exact Task 3 refusal; native 133 remains `UNKNOWN`.
- Task 5: fix round 2/5 (1 addressed, 0 open; commits `32f573e..2d683b2`). Scoped Sol rereview: `PASS / READY`.
- Task 5: complete (commits `b7058de..2d683b2`, review clean).
- Task 6 initial implementation committed as `0a7939ba4`: native DropShadow COMPOSITE/SHADOW_ONLY execution, plan-side DECAL correction, linear-sRGB shadow colorization, public Picture memory/wire replay, and exact B/B−1 atomic recovery. Production compiles pass; W6b 70/70 and W6a preservation 39/39 pass in JUnit before native exit 133 (`UNKNOWN`).
- Task 6 Sol review: `NEEDS_FIXES` with three Important findings — fractional offsets use nearest/floor sampling and renderer-local origin subtraction instead of a frozen linear transform; SHADOW_ONLY retains an unnecessary DropShadowComposite pass/target and therefore the wrong lifetime/budget; public proof omits discriminating SHADOW_ONLY budget/recovery, fractional-origin, translucent composition, sibling-order and both-mode replay cases.
- **R17 — frozen shadow offset and mode topology:** DropShadow offset is a plan-owned target-local linear-sampling transform, including its sampling footprint in desired/required/produced bounds; `:gpu-renderer` consumes sealed coordinates without subtracting origins. `SHADOW_ONLY` terminates directly at the colorized shadow resource and owns no internal composite pass/target; `COMPOSITE` alone owns the source+shadow composite. Cost if wrong: fractional public offsets are quantized, renderer geometry becomes a second authority, and SHADOW_ONLY budgets/lifetimes over-count a non-existent Skia operation.
- Task 6: fix round 1/5 opened (3 Important findings; implementation base `0a7939ba4`).
- Task 6 fix round 1 implemented as `0307b33d8`: plan-owned F64 target-local linear DECAL sampling/footprints, direct SHADOW_ONLY colorized terminal with no internal composite, exact B=336/B−1/nested/late-recovery proof, translucent SrcOver, sibling order, and both-mode Picture memory/wire replay. W6b 74/74, W6a preservation 39/39 and graph contracts pass; native 133 remains `UNKNOWN`.
- Task 6: fix round 1/5 (3 addressed, 0 Critical/Important open; commits `0a7939b..0307b33`). Scoped Sol rereview: `PASS / READY`.
- Task 6 minor (deferred to final branch review): two pre-existing negative `RenderGraphContractTest` fixtures at lines 3398–3402 now omit required sampling/offset payloads and therefore pass vacuously before reaching their announced mutation invariant.
- Task 6: complete (commits `8e4dd83..0307b33`, review clean at blocking severities; one explicit Minor retained for final review).
- Whole-branch Sol review at `19adde550`: `NEEDS_FIXES` with 0 Critical, 7 Important and 3 Minor. Blocking findings are renderer-local spatial conversion, incorrect Table substitution, incorrect fractional SOLID/OUTER/INNER mask formulas, lost invalid-bounds diagnostic, post-allocation archive limits, shallow filter-table immutability, and a Picture test outside the approved public/production-contract gates. Minors cover I32 nomenclature, one malformed negative shadow fixture, and stale status/base counts.
- **R18 — fractional mask semantics:** `MaskFilter.Table` replaces the quantized input coverage with `LUT[coverage]`; it never multiplies the result by input coverage. Blur styles use Porter-Duff coverage equations `SOLID=o+b*(1-o)`, `OUTER=b*(1-o)`, and `INNER=b*o`, with independent public anti-aliased oracles. Cost if wrong: identity tables square AA coverage and three blur styles diverge from Skia precisely at fractional edges.
- **R19 — sealed target-local spatial operands:** every W6b scissor, sample rectangle and source offset crossing into `:gpu-renderer` is final target-local I32 data computed with checked math in `:gpu-plan`; WGSL may transform only within those sealed local domains and native code may not rediscover device origins. Cost if wrong: plan/native become competing geometry authorities and large origins can silently overflow.
- **R20 — bounded deeply immutable filter tables:** archive node counts and Merge fan-out are rejected against `GraphLimits.maxNodes` before list/object allocation; builders reserve within the same bound; mutable geometry payloads are snapshotted on input and on exposure so canonical IDs, bytes and planning cannot change after publication. Cost if wrong: hostile but schema-valid archives allocate/copy near one million entries and callers can mutate wire semantics behind a sealed canonical ID.
- Whole-branch bounded Terra correction wave opened for I1–I7 and M1–M3; no new scope or excluded command is authorized.
- Correction whole-branch round 1, base `2bf3d52`: I2/I3 ajoutent les preuves AA Table/Porter-Duff; I4 rétablit `invalid_bounds`; I5/I6 bornent les archives avant allocation et snapshotent profondément les tables; M1/M2 corrigent nomenclature et fixture DropShadow. `RenderGraphContractTest` est 95/95 exit 0; les sept shards W6b sont 80/80 XML PASS puis native exit 133, donc `UNKNOWN`. La re-review a ensuite laissé I1, la seconde copie privée I5, I7 et M3 ouverts; voir `whole-branch-fix-report.md` pour l'historique et `whole-branch-fix2-report.md` pour la fermeture. Aucun GM, dashboard, render, baseline, `jpg-color-cube` ou suite Skia globale n'a été lancé.
- Scoped Sol rereview of `2bf3d52..0e7b91d`: I2/I3/I4/I6/M1/M2 are closed and I5 is safe but retains one bounded extra copy; two Important findings remain. `FilterComposite` still derives source/destination sampling offsets in the materializer and production validation does not authenticate their values/scissors; three Picture tests still instantiate Render IR/codec types outside the approved public gate. Documentation overstates closure.
- **R21 — authenticated composite sampling:** every `FilterComposite` publishes its final target-local I32 sample offset and scissor, computed with checked plan-side math; production validation authenticates those values, and all Draw/Layer/Picture/GraphTexture native branches consume them verbatim. Cost if wrong: terminal composites remain the last renderer-local geometry authority and extreme origins can diverge or overflow despite the rest of R19.
- Whole-branch correction round 2 opened, bounded to the two remaining Important findings, the bounded-copy Minor, and truthful documentation; no semantic area already closed by round 1 may be reopened.
- Whole-branch correction round 2 implemented as `bf7195792`: all FilterComposite branches consume sealed offset/scissor operands; I5 list ownership is closed; Picture bytes/replay replaces direct codec usage; 97 graph contracts and 81 W6b assertions pass before native exit 133 (`UNKNOWN`). Scoped Sol rereview confirms execution and I5 but keeps two Important findings open: Picture terminal scissor validation is self-referential and one public admission test still imports Render IR limit types.
- **R22 — independently authenticated Picture terminal scissor:** the aggregate/admission record owns the final optional Picture scissor and empty/non-empty state independently of the terminal/pass copies; production validation compares both terminal and FilterComposite operands to that authority. Tests use a valid Picture composite baseline and separate contained-wrong, forged-null, and true-empty-null cases. Cost if wrong: a constructor bug can silently crop or suppress a Picture while the pass and terminal agree with each other and validation accepts both.
- Whole-branch correction round 3 opened, bounded only to R22, removal of the two Render IR limit imports from the public admission shard, and truthful documentation.
- Whole-branch correction round 3 implemented as `8097cd921`: I7 is closed and the validator has causal Picture scissor mutants, with 100 graph contracts and 80 public W6b assertions before native exit 133 (`UNKNOWN`). Scoped Sol rereview confirms I7 and runtime preservation but keeps R22 open: production derives `PictureTerminalScissorAuthorityV1` from the already-built terminal/operands and attaches it after pass publication, so it copies rather than independently authenticates a construction error.
- **R23 — construction-time Picture scissor authority:** derive the Picture terminal scissor authority directly from aggregate admission inputs (deferred clip, admitted composite domain and terminal empty/non-empty decision) before building any terminal or FilterComposite pass. Pass and terminal are both constructed from, then validated against, this earlier immutable fact; no `terminal.toAuthority()` or `operands.toAuthority()` path is permitted. Cost if wrong: validator-only mutants pass while the real producer can duplicate one wrong value into authority, terminal and pass.
- Whole-branch correction round 4 opened with a fresh Terra implementer, bounded exclusively to R23 and truthful documentation.
- Whole-branch correction round 2, base `a1aabe6`: R21 ajoute à chaque `FilterComposite` l'offset et le scissor I32 target-local terminaux, calculés checked, validés relationnellement et consommés verbatim par Draw/Layer/Picture/GraphTexture; les mutants offset/scissor du contrat et le replay public à grande origin couvrent cette frontière. Les tests Picture n'emploient plus table/snapshot/codec/direct decode : mutation, archive oversize et replay passent par `Picture` public. Les listes privées fraîches decoder/builder sont prises en possession sans seconde copie, les callers publics restent défensifs. `RenderGraphContractTest` est 97/97 exit 0; les sept shards W6b sont 81/81 XML PASS, puis `Gradle Test Executor 3` exit 133, donc native **UNKNOWN**. Le détail RED→GREEN et les fichiers sont dans `whole-branch-fix2-report.md`; exclusions inchangées, aucun GM/dashboard/render/baseline/score/`jpg-color-cube`/suite Skia globale exécuté.
- Whole-branch correction round 3, base `6453b06`: R22 ajoute l'admission distincte du scissor terminal Picture sur l'aggregate (scissor optionnel, admis et vide/non-vide) et les mutants contractuels contained-wrong, forged-null et true-empty-null. Le test admission paramétré par `GraphLimits`/`SceneCaptureLimits` est retiré : les refus/recovery d'archives oversize restent prouvés par `Picture.fromByteArray`, bytes et replay publics. `RenderGraphContractTest` est 100/100 exit 0; les sept shards publics W6b sont 80/80 XML PASS, puis un `Gradle Test Executor` exit 133, donc native **UNKNOWN**. La re-review a toutefois laissé R22 ouvert : le producteur de l'autorité copiait encore terminal/operands après leur construction. Voir `whole-branch-fix3-report.md`; exclusions inchangées, aucun GM/dashboard/render/baseline/score/`jpg-color-cube`/suite Skia globale exécuté.
- Whole-branch correction round 4, base `b37bf29`: R23 matérialise `PictureTerminalScissorAuthorityV1` dans `admitPictureTerminalScissor`, directement depuis le clip différé, le domaine composite admis et la décision terminale vide/non-vide, avant operands, terminal ou `FilterComposite`. `PictureTerminalAdmissionV1` transmet ce fait immutable aux deux copies authentifiées ; les producteurs postérieurs `terminal?.toTerminalScissorAuthority()` et `operands.toTerminalScissorAuthority()` sont supprimés. La fixture causale emploie un vrai `W6aLayerPlanCompiler` Picture filtré, et sa mutation temporaire vers une autorité `null` échoue avant restauration. `RenderGraphContractTest` est 102/102 exit 0; les sept shards publics W6b sont 80/80 XML PASS, puis `Gradle Test Executor 21` exit 133, donc native **UNKNOWN**. Voir `whole-branch-fix4-report.md`; exclusions inchangées, aucun renderer/GM/dashboard/render/baseline/score/`jpg-color-cube`/suite Skia globale exécuté.
- Final scoped Sol rereview of R23: `PASS / READY` with 0 Critical, 0 Important and 0 Minor; filtered, unfiltered and empty Picture terminal producers derive authority before terminal/pass copies, and no output-to-authority producer remains.
- **R24 — sealed W4e material origin:** imported W4c/W4d `RenderPass` rows and `PlanW4eGeometryBindingV1` both snapshot the plan-owned target device origin before lowering; `bindSources` preserves it and the W4e renderer consumes that value for RenderPass and StencilCover without recreating a target-origin map. Cost if wrong: translated path/clip layers refuse after W6b removes renderer-local geometry authority, even though W6b filters themselves remain correct.
- Final verification exposed four W6a `StencilCover` refusals from the first R19 correction. The root cause was a missing W4e binding origin, not a FilterComposite/R23 failure. Fix `e0414a906` closes R24; scoped Sol rereview is `PASS / READY` with 0 findings.
- Fresh controller verification at `e0414a906`: `RenderGraphContractTest` 102/102 and Gradle exit 0; W6a preservation 39/39 XML PASS and W6b public 80/80 XML PASS, with both GPU executors subsequently exiting 133 and therefore remaining native **UNKNOWN**. `git diff --check` and worktree status are clean. Fonts, codecs, GMs, dashboard, renders, baselines, scores, global Skia and `jpg-color-cube` were not exercised.
