package org.skia.core

/**
 * C++ original:
 * ```cpp
 * template <typename T, class Enable = void>
 * struct sk_strip_enum {
 *     typedef T type;
 * }
 * ```
 */
public open class SkStripEnum<T, Enable>
