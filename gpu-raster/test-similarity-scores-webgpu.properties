# gpu-raster GM cross-test similarity scores (vs original-888 reference PNGs)
#
# Each line records the per-channel similarity (tolerance = 8) of a GM
# rendered through SkWebGpuDevice (gpu-raster) compared to the upstream
# Skia DM reference PNG. The format mirrors :cpu-raster's
# test-similarity-scores.properties so a future SimilarityTracker can
# read both files with the same parser.
#
# **G6.0 colorspace post-process applied (since #486).** WebGpuSink
# applies sRGB-encoded -> linear -> Rec.2020-primaries -> Rec.2020-OETF
# per pixel before comparison. Reference PNGs are encoded in
# `DM unified Rec.2020` (Rec.2020 primaries + Rec.2020 transfer fn ;
# see TestUtils.DM_REFERENCE_COLOR_SPACE), so this aligns the
# comparison space. Scores jumped 7-29 points after G6.0 -- proof
# that colorspace was the dominant drift source for the cross-tests
# (cf. G3.1.1 BigRectGM remaining at 70.7%).
#
# Tolerance 8 still permits sub-pixel edge convention drift (AA stroke
# corners, hairline rounding, etc.). 6/24 GMs hit 100 % similarity now ;
# the 18 below 100 % are blocked by a mix of AA stroke corner edges,
# sub-pixel stroke widths, non-AA-vs-AA-reference drift, AA cubic
# Bezier edge fractional coverage, extreme self-intersecting concave
# cells, etc. G3.3b.3d (two-pass AA cover) closed the stencil-and-cover
# outside-half AA loss. G3.4.1 unlocked drawPath stroke style via
# SkStroker. G4.1 unlocked the first SkLinearGradient pipeline.

ThinRectsGM=100.00
ClipStrokeRectGM=100.00
# ScaledRectsGM : drawPaint bg + clipRect axis-aligned + 2 large rects
# under rotated/skewed CTM (G3.3a polygon path) + kPlus blend (G3.3a.1).
# After G6.0 colorspace transform : perfect match (100 %).
ScaledRectsGM=100.00
# BigRectGM : 240+ rects with stroke (sw=0 hairline + sw=1 thin), AA on/off,
# clipRect axis-aligned, extreme coords. After G6.0 : 99.90 % (40 pixels
# off out of 40625 -- AA hairline + AA stroke corner edge cases, will be
# closed by G3.4 SkStroker generic).
BigRectGM=99.90
# ThinStrokedRectsGM : 7 x 8 grid of axis-aligned stroked rects with sub-
# pixel stroke widths (4, 2, 1, 0.5, 0.25, 0.125, 0). After G6.0 : 94.21 %
# (sub-pixel stroke edge conventions vs raster's strokeRectAA still drift
# on the thin widths -- G3.4 territory).
ThinStrokedRectsGM=94.21
# Skbug12244GM : single drawPath, non-AA fill, MULTI-CONTOUR line-only.
# After G3.3b.2b stencil-and-cover : 99.29 % (the hole between the two
# contours is correctly excluded via winding count). Remaining 0.71 % =
# ~160 edge pixels where the reference (rasterized with AA) has
# fractional coverage but our non-AA fill is binary. Closed by G3.3b.3
# (AA stencil-and-cover) or AA polygon work.
Skbug12244GM=99.29
# BatchedConvexPathsGM : 10 single-contour convex cubic-Bezier polygons
# at shrinking scales, translucent SrcOver stacking on black. Pure
# feature stack exercised : G3.3b.1 cubic flattening + G3.3b.2a AA
# polygon coverage + G2.1 translucent SrcOver + G6.0/G6.1 colorspace.
# Score : 99.94 % (~160 pixels drift on AA edges, sub-channel).
BatchedConvexPathsGM=99.94
# FiddleGM : empty onDraw, renders only the default white background.
# Exercises the no-draws-only-background-clear path through the
# G6.0/G6.1 sRGB -> Rec.2020 present-pass transform. Round-trip exact.
FiddleGM=100.00
# ClipDrawDrawGM : crbug.com/423834 repro -- clipRect + drawRect + drawRect
# sequences testing integer-edge rounding consistency between clipRect
# and drawRect. Pure axis-aligned non-AA fill rects on the rect
# fast-path (G1.2 scissor + G2.3a non-AA fill). Perfect match.
ClipDrawDrawGM=100.00
# Bug7792GM : 16 line-only paths exercising moveTo/close edge cases for
# the non-AA fill rasterizer (skbug.com/40039046). Multi-contour kWinding
# fill via stencil-and-cover (G3.3b.2b). 99.99 % -- ~70 edge pixels
# differ sub-channel because the reference is AA-rasterised while our
# fill is binary coverage. Closed by G3.3b.3 (AA stencil-and-cover).
Bug7792GM=99.99
# ConcavePathsGM : 29 concave / self-intersecting / coincident-edge cells,
# all painted with isAntiAlias = true. After the G3.3b.3a follow-up,
# single-contour concave paths are detected via cross-product convexity
# and routed through the same AA stencil-and-cover path as multi-contour
# (the fan-tessellation winding count naturally cancels in the concave
# pockets). G3.3b.3d (two-pass AA cover : a second cover sub-draw with
# the stencil compare op flipped + `clamp(0.5 - minDist, 0, 1)` paints
# the outside half of the AA falloff) lifted this from 98.90 to 99.31 %.
# Residual drift is a few cells with extreme self-intersection where
# fan-tess winding cancellation isn't enough.
ConcavePathsGM=99.31
# FillTypeGM : 4x4 grid -- two overlapping circles (multi-contour) drawn
# under each of the 4 fill types (kWinding, kEvenOdd, kInverseWinding,
# kInverseEvenOdd) at 2 scales, each in non-AA AND AA passes. Exercises
# the complete G3.3b.3b fill-rule matrix. Score : 99.55 %. The G3.3b.3d
# two-pass AA cover redistributes coverage across the boundary but the
# residual mismatch is dominated by the non-AA cells (binary coverage
# vs the AA reference circle edges), so the per-pixel similarity stays
# flat at this tolerance even though the AA cells are visually closer.
FillTypeGM=99.55
# AnalyticAntialiasInverseGM : single drawPath of a 30-radius circle at
# (100,100) with kInverseWinding fill, AA on, red. Visible field is the
# complement of the disc. Exercises single-contour convex inverse fill
# (G3.3b.3b -- stencil Equal 0 + full-viewport cover). Score : 99.98 %.
AnalyticAntialiasInverseGM=99.98
# PathInvFillGM : 450 x 220, 4 cells = 2 (doclip) x 2 (AA) of a circle
# at (50,50) r=40 with kInverseWinding fill, drawn twice under nested
# axis-aligned clipRects. Exercises convex inverse fill (G3.3b.3b) under
# nested clipRects, non-AA AND AA. Score : 99.52 % (stale floor was
# 99.50, ratcheted on the G3.4.3 branch ; the GM does not exercise
# hairlines, the floor was just trailing reality).
PathInvFillGM=99.52
# Crbug1472747GM : 400 x 400 single drawPath -- inner + outer ovals at
# r=31000 / r=31005 with kEvenOdd fill producing a thin ring. Each oval
# built from two 180-degree arcTo half-arcs. Tests kEvenOdd on multi-
# contour input with conic-flattened arcs at extreme radius (G3.3b.3b
# on top of G3.3b.1 conic flattening). Score : 98.16 %.
Crbug1472747GM=98.16
# ConvexLineOnlyPathsFillGM : 512 x 512 -- 20 convex line-only polygons
# (narrow rects, trapezoids, teardrops, n-gons up to 100 sides) each
# drawn 7 times at scales {1, .75, .5, .25, .1, .01, .001} alternating
# CW/CCW, AA on, alternating black/white fill. Plus three crbug repros.
# Pure AA polygon shader workout (single-contour convex line-only fill).
# Score : 98.75 %.
ConvexLineOnlyPathsFillGM=98.75
# CubicStrokeGM : 3 near-identical AA stroked cubic Bezier paths with
# sub-1 % stroke-width variation (1.0720 / 1.0721 / 1.0722), each
# translated 10 px. Exercises G3.4.1 SkStroker integration : the open
# cubic gets turned into a single closed outline (left + cap +
# reversed-right + cap) which then routes recursively through drawPath
# as a fill via the concave-single-contour AA path (G3.3b.3a.2). First
# stroke GM exercising the SkStroker on non-rect paths -- previously
# threw 'only fill-style paths supported'. Score : 98.57 %.
CubicStrokeGM=98.57
# SmallArcGM (G3.4.2) : one red AA stroked cubic-Bezier quarter-arc at
# strokeWidth=120 under translate(-400,-400)+scale(8,8). Exercises
# SkStroker.resScale at CTM 8x on a single open cubic ; the outline is
# a single closed concave contour routed through AA stencil-and-cover
# (G3.3b.3a.2). Score : 99.80 %.
SmallArcGM=99.80
# ArcOfZorroGM (G3.4.2) : 200 stroked open-arc paths
# (drawArc(useCenter=false), strokeWidth=35) of slowly-varying sweep,
# random colour, on a 1000x1000 grey background drawn via drawPaint.
# Heavy stroker throughput on cubic-flattened arcs. Score : 99.73 %.
ArcOfZorroGM=99.73
# Bug6987GM (G3.4.2) : a 1px-scale closed triangle stroked at
# strokeWidth=0.0001 under scale(50000,50000). Pure stress on
# SkStroker.resScale at extreme CTM scale -- without proper res-scale
# the outline flattens to a polygon at low resolution. Score : 99.77 %.
Bug6987GM=99.77
# ScaledStrokesGM (G3.4.2) : 4 shapes (cubic rounded-square, circle,
# rect, line) x 4 scales (1x..4x) x 2 AA modes, stroked at
# strokeWidth=4/scale so device-space width stays constant. Score :
# 96.49 % -- drift on non-AA cells' binary coverage vs the AA reference
# and sub-pixel stroke edges on the line/rect rows.
ScaledStrokesGM=96.49
# AddArcGM (G3.4.2) : concentric stroked open arcs of 345 deg sweep,
# inset by strokeWidth+4 per iteration, AA + strokeWidth=15. Arc paths
# built via SkPathBuilder.addArc. Score : 93.30 % -- same AA stroke edge
# convention drift as observed on CPU raster (91.91 %).
AddArcGM=93.30
# Bug12866GM (G3.4.4) : skbug.com/40043963 repro. Tiny quad-only closed
# contour drawn twice : left at default resScale=1, right via explicit
# SkStroker.fromPaint(paint, resScale=1200).stroke + fill. Stress on the
# stroker's recursion limit at extreme subdivision. 128 x 64 reference
# image (only 8 192 px) -- per-pixel drift amplified. Score : 95.24 %.
Bug12866GM=95.24
# InnerJoinGeometryGM (G3.4.4) : 8 acute-angle line-triangles stroked at
# strokeWidth=100 with kMiter_Join, overlaid with a red 0-width skeleton.
# Originally a regression repro for skbug.com/40043052 (missing
# inner-join geometry on highly-acute corners). Score : 98.71 %.
InnerJoinGeometryGM=98.71
# QuadCapGM (G3.4.4) : two AA-stroked quadratic Beziers at strokeWidth=0
# (hairline) -- one kButt_Cap extended by pi/8 along the tangent, one
# kRound_Cap on the original quad. Butt vs round cap geometry comparison
# on hairlines via the G3.4.3 `1 / resScale` synthesis path. Score :
# 99.80 %.
QuadCapGM=99.80
# ZeroLineStrokeGM (G3.4.4) : 3 zero-or-tiny-length strokes at
# strokeWidth=20 with kRound_Cap, AA, each with duplicate consecutive
# points (collinear / degenerate segments). Tiny 90 x 120 image, so
# round-cap arc edges amplify per-pixel drift. Score : 94.05 %.
ZeroLineStrokeGM=94.05
# StrokerectAnisotropicGM (G3.4.4) : crbug.com/935303 repro. 4 x 2 grid
# of {miter, miter-half-pixel, bevel, bevel-half-pixel} x {AA, non-AA},
# 1000 x 20 source rects routed through drawPath under scale(0.03, 2)
# anisotropic CTM. Stress on resScale + join geometry when source vs
# device aspect ratios diverge. Score : 98.11 %.
StrokerectAnisotropicGM=98.11
# RectPolyStrokeGM (G3.4.4) : 48 cells -- 3 joins (miter / round /
# bevel) x 4 rect shapes (including degenerate 0 x H, W x 0, 0 x 0) x 2
# rotations x 2 procs (drawRect vs drawPath). Each cell layered with a
# 0-width green hairline overlay to expose drawRect-fast-path vs
# path-stroker emission divergence. Score : 98.21 %.
RectPolyStrokeGM=98.21
# ShallowGradientLinearGM (G4.1) : 800 x 800 single drawRect filled
# with a SkLinearGradient (kClamp tile mode) from (0, 0) -> (800, 800),
# colours 0xFF555555 -> 0xFF444444. First gradient GM on GPU --
# exercises the new linear-gradient pipeline end-to-end. Score :
# 100.00 % (byte-exact -- the gradient values lerp through a tight
# 0x44..0x55 range that the present-pass colorspace transform happens
# to keep byte-stable). Floor pinned at 90 % to absorb future drift if
# the GPU pipeline picks up sub-LSB variation under different drivers.
ShallowGradientLinearGM=100.00
# ShallowGradientLinearNoditherGM (G4.1.1) : same GM body as the
# dithered variant -- 800 x 800 single drawRect filled with a kClamp
# linear gradient 0xFF555555 -> 0xFF444444. Only the upstream reference
# differs (dither off). Byte-exact match. Score : 100.00 %.
ShallowGradientLinearNoditherGM=100.00
# AnalyticGradientShaderGM (G4.1.1) : 8 x 4 grid of kClamp linear
# gradients with 1..8 interpolation intervals and 2..16 colour stops
# per cell, mixing smooth transitions with hardstops. Stress-tests
# the upper-bound (16) of MAX_GRADIENT_STOPS and the duplicate-position
# hardstop dispatch. Byte-exact match. Score : 100.00 %.
AnalyticGradientShaderGM=100.00
# Crbug938592GM (G4.1.1) : 150 x 30 rect with a 3-stop hardstop linear
# gradient (blue->red->green at 9/20 and 11/20), mirrored 4 ways via
# translate + scale(+/-1, +/-1). Validates the path.isRect gate under
# negative-scale axis-aligned CTM. Score : 99.80 %.
Crbug938592GM=99.80
# ConvexPathsGM (G4.1.1) : 38 convex paths (rect / circle / oval / rrect
# / cubic / quad / conic / arc / line + many degenerate variants) tiled
# in a 5-column grid under axis-aligned scale(2/3) + per-cell translate
# CTM, AA fill, pseudo-random opaque colours. Heavy convex-single-
# contour AA fill workout exercising curve flattening + AA polygon
# coverage on dozens of shapes in a single GM. Score : 99.85 %.
ConvexPathsGM=99.85
# ShallowGradientRadialGM (G4.2) : 800 x 800 single drawRect filled
# with a SkRadialGradient (kClamp tile mode) centred at (400, 400),
# radius 400, colours 0xFF555555 -> 0xFF444444. First radial-gradient
# GM on GPU -- exercises the new radial-gradient pipeline end-to-end.
# Byte-exact match. Score : 100.00 %. Floor pinned at 90 % to absorb
# future drift if the pipeline picks up sub-LSB variation under
# different drivers.
ShallowGradientRadialGM=100.00
# ShallowGradientRadialNoditherGM (G4.1.3) : same GM body as the
# dithered variant -- 800 x 800 single drawRect filled with a kClamp
# radial gradient 0xFF555555 -> 0xFF444444 centred at (400, 400)
# radius 400. Only the upstream reference differs (dither off). Byte-
# exact match. Score : 100.00 %.
ShallowGradientRadialNoditherGM=100.00
# ClampedGradientsGM (G4.1.3) : 640 x 510 canvas with 0xFFDDDDDD bg
# (drawPaint) and a 100 x 300 drawRect at (20, 20) filled with a 5-stop
# kClamp SkRadialGradient (red/green/blue/white/black) centred at
# (0, 300) radius 200. The radial centre is outside the drawn rect so
# every pixel sees a non-trivial radial distance -- no t = 0 shortcut.
# Byte-exact match. Score : 100.00 %.
ClampedGradientsGM=100.00
# HardstopGradientShaderGM (G4.1.3) : 8 x 3 grid of linear gradients
# on rect. Columns iterate through SkTileMode (kClamp / kRepeat /
# kMirror) ; rows iterate through 8 stop configurations (no positions
# / evenly spaced / hard stops at various positions, 2..5 stops).
# First multi-tile-mode GM cross-test exercising kRepeat and kMirror
# on real geometry alongside kClamp. Byte-exact match. Score :
# 100.00 %.
HardstopGradientShaderGM=100.00
# TestGradientGM (G4.1.3) : 800 x 800 mixed-primitive smoke test --
# filled rect with a kClamp blue->yellow linear gradient (partial t
# sweep), a filled RRect oval, a filled circle, and a stroked round-
# rect (radii 10, AA, strokeWidth 4). Exercises gradient partial-sweep
# alongside drawCircle / drawRRect / drawRoundRect in one frame.
# Score : 99.96 %.
TestGradientGM=99.96
# FillrectGradientGM (G4.1.3) : 120 x 540 canvas, 2-col x 9-row grid
# of 50 x 50 rect cells. Each row is the same stop list rendered once
# with SkLinearGradient and once with SkRadialGradient (kClamp), both
# on rect (in scope). Stops cover all corner cases : 2/3-stop endpoints,
# sub-range, single-stop, disjoint via duplicate position, ignored
# duplicates, unsorted input. Score : 95.77 % -- drift on the sub-range
# (row 3) and disjoint-stop rows (7-9) where the lerp arithmetic
# differs sub-LSB from the reference, plus radial-cell edges.
FillrectGradientGM=95.77
# FillCircleGM (G4.1.3) : 520 x 520 spiralling stack of concentric AA
# ovals under scale(20, 20) + translate(13, 13) CTM. Each oval is
# filled with a deterministic 565-quantised random colour. Pure AA
# oval fill workout under axis-aligned scale -- no shader, no stroke,
# no clip. Score : 98.58 % -- residual drift on AA oval boundary
# pixels where the reference's fractional coverage differs sub-LSB.
FillCircleGM=98.58
# BeziersGM : 2 panes of 10 random AA-stroked Bezier paths each --
# top moveTo + 2 x quadTo, bottom moveTo + 2 x cubicTo, stroke widths
# spanning ~1-25 px with per-path random opaque colors. Exercises the
# G3.4.1 SkStroker integration on quad + cubic flatten flows at a
# variety of stroke widths. Score : 96.98 % -- residual drift on AA
# stroke edge convention vs raster's strokeRectAA at the larger widths.
BeziersGM=96.98
# B119394958GM : blue filled circle + green stroked circle +
# red round-cap stroked arc (start 0 deg, sweep 110 deg, useCenter
# false). First GM in scope combining drawArc(useCenter = false) with
# kRound_Cap -- the round-cap endpoints become half-circles via
# SkStroker (G3.4.1). Score : 93.82 %. 100 x 100 reference, residual
# drift on the round-cap arc edges where the stroker's polyline
# flattening of the cap's half-circle diverges sub-LSB from upstream.
B119394958GM=93.82
# ArcCircleGapGM : stroked circle + stroked tangent-arc, both at huge
# radius (~1097). Default hairline stroke (strokeWidth = 0) routes
# through G3.4.3 `1 / resScale` synthesis before SkStroker. Score :
# 99.14 % (250 x 250 reference, 539 pixels drift on the sub-pixel
# gap region between the circle and the tangent-arc -- the regression
# this GM probes).
ArcCircleGapGM=99.14
# AnalyticAntialiasConvexGM : 5 configurations stressing the analytic-AA
# convex-fill path under a 1-degree rotated CTM. First GM landed after
# G1.4 unblocked canvas.clear() on non-raster devices. Score : 99.90 %.
AnalyticAntialiasConvexGM=99.90
# ArcToGM : 500 x 600 SVG-style arcTo workout : 8-arc loop section
# (2 rotations x 2 oval heights x small-CW + large-CCW), 4-coloured chord
# (kSmall/kLarge x kCW/kCCW), and 2 zero-length round-cap degenerates.
# Pure AA stroke + arc-flattening through G3.4.1 SkStroker on cubic-
# flattened arcs. Score : 96.38 %.
ArcToGM=96.38
# HairlineSubdivGM : 512 x 256, 4 overlaid AA hairline quadratic Beziers
# at progressively-shifted origins. Each draw exercises a different
# subdivision count in the AA hairline Bezier flattener via G3.4.3
# `1 / resScale` hairline synthesis + G3.4.1 SkStroker. Score : 97.50 %.
HairlineSubdivGM=97.50
# PathArcToSkbug9077GM : 200 x 200 AA-stroked multi-contour path : 3 lineTo
# + close + tangent arcTo(radius=60) after the close. Regression for the
# implicit moveTo after close emitting a stale starting point. Cubic-
# flattened arc through G3.4.1 SkStroker. Score : 99.00 %.
PathArcToSkbug9077GM=99.00
# PathSkbug11859GM : 512 x 512 red AA-filled two-subpath path under
# scale(2, 2). Regression for clipping when coordinates near the bitmap
# edge (-2) interact with the rasterizer's edge arithmetic. Multi-contour
# kWinding fill via G3.3b.3a stencil-and-cover. Score : 99.95 %.
PathSkbug11859GM=99.95
# ShallowGradientSweepNoDitherGM (G-suivi round 12) : 800 x 800 single
# drawRect filled with a kClamp SkSweepGradient centred at (400, 400),
# colours 0xFF555555 -> 0xFF444444. Mirrors the linear / radial shallow-
# gradient cross-tests but routes through the sweep pipeline (G4.3). The
# rect is axis-aligned and the gradient colours are byte-stable through
# the G6.0/G6.1 Rec.2020 transform on this near-grey ramp. Byte-exact
# match. Score : 100.00 %.
ShallowGradientSweepNoDitherGM=100.00
# ShallowGradientSweepDitherGM (G-suivi round 12) : same body as the
# nodither variant -- 800 x 800 single drawRect filled with a kClamp
# SkSweepGradient at colours 0xFF555555 -> 0xFF444444. Only the upstream
# reference differs (dither on). Our rasterizer never applies dither so
# GPU output is identical to the no-dither path ; the dithered reference
# still matches byte-exact on this near-grey ramp because the dither
# delta stays sub-LSB. Score : 100.00 %.
ShallowGradientSweepDitherGM=100.00
# SweepTilingGM (G-suivi round 12) : 690 x 512, 3 x 4 grid of 160 x 160
# drawRects each filled with a SkSweepGradient. Rows iterate the tile
# modes {kClamp, kRepeat, kMirror} (G4.3 + G4.3.1 unlocked all three on
# rect with axis-aligned CTM). Columns iterate four startAngle/endAngle
# pairs producing partial / full / overflowing sweeps. First cross-test
# GM exercising kRepeat and kMirror on sweep alongside kClamp. Byte-
# exact match. Score : 100.00 %.
SweepTilingGM=100.00
# PolygonsGM (G-suivi round 12) : 840 x 1140 grid of 8 polygons
# (triangle, trapezoid, diamond, octagon, 32-edge circle approx, concave
# quad, stairs, 5-point star) drawn under 11 rows : 3 joins x 3 stroke
# widths (0/10/40) + strokeAndFill + fill rows. AA on, axis-aligned
# translate CTM, no shader. Heavy stroker + concave AA fill workout via
# G3.4.1 SkStroker + G3.3b.3a stencil-and-cover. Score : 98.59 %.
PolygonsGM=98.59
# ShallowGradientConicalDitherGM (G-suivi round 13) : dither-on twin of
# the existing ShallowGradientConicalNoDitherGM (also 100.00 %). 800 x 800
# kRadial conical gradient (start == end centre) between near-grey
# stops. Our rasterizer never applies dither, so GPU output is identical
# to the no-dither sibling ; the dithered upstream reference still
# matches byte-exact on this near-grey ramp. Score : 100.00 %.
ShallowGradientConicalDitherGM=100.00
# ConicalGradients2ptInsideDitherGM (G-suivi round 13) : dither-on twin
# of the existing ConicalGradients2ptInsideGM (nodither variant, 80 %
# floor). 840 x 815 grid of 4 columns x 7 rows of 100 x 100 conical
# gradient rects, kClamp. Exercises kRadial + focal-inside well-behaved
# sub-cases (G4.4.1 / G4.4.2). Higher score than the nodither sibling
# because the dither reference happens to land closer to our
# deterministic output on the cells where the focal sub-case still
# falls through to solid colour. Score : 97.51 %.
ConicalGradients2ptInsideDitherGM=97.51
# GradientsGM (G-suivi round 13) : 840 x 815 grid of 6 columns x 5 rows
# of 100 x 100 drawRects. Rows iterate over five gradient types : linear
# (G4.0), radial (G4.1), sweep (G4.3), 2-conical focal-inside (G4.4.1),
# 2-conical focal-outside. Columns iterate over six colour-stop
# configurations. All kClamp, axis-aligned CTM. The last row
# (focal-outside) falls through to the solid-color machinery, taking
# ~10 % of pixels off the textual band. Score : 90.12 %.
GradientsGM=90.12
# GradientDirtyLaundryGM (G-suivi round 13) : 640 x 615, three stacked
# 100 x 100 drawRects (linear / radial / sweep), each filled with a
# 40-stop kClamp gradient replaying {R, G, B, W, K} eight times. Pure
# rect + axis-aligned CTM ; all three shaders in scope. Drift
# concentrated on the dense sampler's per-stop lerp boundaries -- a few
# LSB of channel jitter per row leaves a couple of thousand pixels
# outside the textual tolerance band. Score : 94.48 %.
GradientDirtyLaundryGM=94.48
# TinyBitmapGM (G-suivi round 14) : 100 x 100 canvas, drawPaint with a
# bitmap shader sourcing a 1 x 1 translucent-red bitmap (kRepeat /
# kMirror, paint.alphaf = 0.5). G5.2 routes drawPaint(paint with
# SkBitmapShader) through drawPath(rect) so the bitmap-shader gate
# fires. 1 x 1 source collapses the tile mode to identity ; paint alpha
# modulation is a deterministic per-pixel multiply. Byte-exact match.
# Score : 100.00 %.
TinyBitmapGM=100.00
# TilemodesAlphaGM (G-suivi round 14) : 512 x 512 canvas, 4 x 4 grid of
# 126 x 126 axis-aligned drawRect cells each filled by an image shader
# sampling mandrill_64.png. Rows / columns iterate the Y / X tile mode
# over {kClamp, kRepeat, kMirror, kDecal} ; paint colour (0,0,0,0.5)
# modulates the shader output (crbug.com/957275 regression). All 16
# cells in-scope after G5.2 (axis-aligned CTM + translate-only local
# matrix + rect). Byte-exact match. Score : 100.00 %.
TilemodesAlphaGM=100.00
# DrawBitmapRect3 (G-suivi round 14) : 640 x 480 canvas, single
# drawImageRect of a 3 x 3 colour-stamped bitmap with src = (0.5, 0.5,
# 2.5, 2.5) into dst = (100, 100, 300, 200), nearest sampling, kStrict.
# The partial src rect produces a 2 x 2 grid of coloured stripes scaled
# into the 200 x 100 dst rect. Pure drawImageRect routing (G5.1), no
# paint shader. Byte-exact match. Score : 100.00 %.
DrawBitmapRect3=100.00
# BitmapRectTestGM (G-suivi round 14) : 320 x 240 canvas. CPU-built
# 60 x 60 bitmap (filled triangle + 1-px stroked outline) drawn three
# times : drawImage at (150, 45) ; drawImageRect under
# scale(0.472560018) into (100, 100, 228, 228) ; drawImage at
# (-310, 45) under scale(-1, 1). Upstream regression for a bug that
# drew the right column of src pixels twice under scale. All three
# draws in scope (axis-aligned CTM + reflection). ~1.5 % drift along
# the stroked outline (sub-pixel sampling edge antialiasing). Score :
# 98.50 %.
BitmapRectTestGM=98.50
# BitmapRectRoundingGM (G-suivi round 14) : 640 x 480 canvas, uniform
# scale(0.9, 0.9) CTM, two stacked axis-aligned draws : drawRect((1,1,
# 110,114)) filled red ; drawImageRect(blueBitmap, ..., same rect)
# fully covering the red. Upstream regression for a precision bug that
# leaked a 1-pixel red seam at the bottom under sub-pixel scale.
# Byte-exact match -- our drawImageRect rounding aligns with upstream.
# Score : 100.00 %.
BitmapRectRoundingGM=100.00
