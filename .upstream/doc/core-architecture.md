# Core Architecture

> TODO — document Skia's foundational types and cross-cutting utilities:
> reference counting (`SkRefCnt`, `SkWeakRefCnt`), data containers (`SkData`,
> `SkStream`, `SkSpan`, `SkString`), arena/block allocators
> (`SkArenaAlloc`, `SkBlockAllocator`), error handling, debug macros, and
> the base utilities under `src/base/` and `include/private/`.
