# Animated Images

> TODO — document `SkAnimatedImage` (`include/android/SkAnimatedImage.h`,
> `src/android/SkAnimatedImage.cpp`), `SkCodecAnimation`, the
> `SkFrameHolder` model used by GIF/WebP/AVIF/APNG decoders, and the
> Android NDK animated-image surface helpers.
