# Metal Backend

> TODO — document the Apple Metal backend used by Ganesh
> (`src/gpu/ganesh/mtl/`) and Graphite (`src/gpu/graphite/mtl/`):
> `GrMtlBackendContext`, MSL emission, `MTLPipelineState` caching, and
> integration with macOS / iOS surface delivery (CAMetalLayer,
> IOSurface, MTLDrawable).
