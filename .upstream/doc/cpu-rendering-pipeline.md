# CPU Rendering Pipeline

> TODO — document the CPU rasterizer: `SkRasterPipeline` and its stages
> (`src/opts/SkRasterPipeline_opts.h`), blitters (`SkBlitter`,
> `SkBlitMask`, `SkBlitRow`), scan conversion, anti-aliasing
> (`SkAAClip`, `SkAnalyticEdge`), dithering, the SIMD kernels selected
> per-CPU under `src/opts/`, and how the pipeline is driven from
> `SkBitmapDevice`.
