# Capture & Debugging

> TODO — document the capture / debug surface: `src/capture/` (`SkCapture`,
> `SkCaptureCanvas`, `SkCaptureManager`), the audit trail (`GrAuditTrail`),
> `SkATrace` / Perfetto integration, the debugger bindings exposed via
> CanvasKit (`debugger_bindings.cpp`), and the trace-event tracer
> (`SkEventTracer`, `SkTraceEventPhase`).
