# SkResources

> TODO — document `modules/skresources/`: the `ResourceProvider`
> interface used by Skottie and the SVG module to fetch images, fonts,
> and external assets, including the file / data-URI / multi-frame image
> implementations.
