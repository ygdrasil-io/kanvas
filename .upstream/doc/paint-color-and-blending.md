# Paint, Color & Blending

> TODO — document `SkPaint` (the configuration object passed to every
> draw call), the color types (`SkColor`, `SkColor4f`, `SkAlphaType`,
> `SkColorType`), `SkColorFilter`, `SkBlender`/`SkBlendMode`/`SkCoverageMode`,
> and the un-premultiply helpers.
