# SVG Canvas

> TODO — document `src/svg/` and `include/svg/SkSVGCanvas.h`: the canvas
> implementation that emits SVG markup as drawing commands are issued.
> This is the SVG **output** path; SVG **input** is covered by the
> [SVG Module](svg-module.md).
