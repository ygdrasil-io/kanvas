# SkSL Shading Language

> TODO — document `src/sksl/` and `include/sksl/`: the lexer
> (`SkSLLexer`), parser, IR (`ir/`), analysis passes (`analysis/`),
> transforms (`transform/`), constant folding, inlining, the built-in
> module system, and the code generators (`codegen/`) targeting GLSL,
> Metal SL, SPIR-V, WGSL, and HLSL. See also
> [Runtime Effects](runtime-effects.md) for SkSL exposed at the public API.
