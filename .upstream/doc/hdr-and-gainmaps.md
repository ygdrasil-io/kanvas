# HDR & Gainmaps

> TODO — document HDR support: `SkGainmapInfo`, `SkGainmapShader`,
> `SkHdrMetadata`, AGTM (Adaptive Gainmap Tone Mapping — see
> `src/codec/SkHdrAgtm*`), and the JPEG-gainmap encode path
> (`SkJpegGainmapEncoder`).
