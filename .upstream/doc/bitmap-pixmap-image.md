# Bitmap, Pixmap & Image

> TODO — document the pixel-container hierarchy: `SkPixmap` (non-owning
> view), `SkBitmap` (mutable, CPU-side), `SkImage` (immutable, possibly
> GPU-backed), `SkPixelRef`, `SkMallocPixelRef`, `SkImageInfo`,
> `SkImageGenerator`, `SkTiledImageUtils`, and `SkYUVAInfo`/`SkYUVAPixmaps`.
