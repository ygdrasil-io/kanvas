# Image Decoders

> TODO — document the codecs in `src/codec/` and their public headers under
> `include/codec/`: `SkCodec`, `SkAndroidCodec`, plus per-format decoders
> for JPEG, PNG (libpng + Rust), WebP, GIF, BMP, ICO, WBMP, AVIF (libavif
> and crabby-avif), JPEG-XL, and RAW. Also covers `SkEncodedImageFormat`,
> `SkEncodedOrigin`, EXIF parsing (`SkExif`), and the frame-holder model
> for animated formats.
