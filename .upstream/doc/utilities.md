# Utilities

> TODO — document the helper APIs in `include/utils/` and `src/utils/`:
> `SkParse` / `SkParsePath`, `SkShadowUtils`, `SkCustomTypeface`,
> `SkCamera` (3D transforms), `SkPaintFilterCanvas`, `SkOrderedFontMgr`,
> `SkCanvasStateUtils`, `SkEventTracer`, `SkTextUtils`, plus the macOS
> `SkCFObject` wrapper.
