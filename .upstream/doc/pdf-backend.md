# PDF Backend

> TODO — document `src/pdf/`: the PDF document writer, including
> `SkPDFDevice`, font subsetting and CID/ToUnicode tables
> (`SkPDFFont`, `SkPDFSubsetFont`, `SkPDFMakeToUnicodeCmap`),
> gradient shaders (`SkPDFGradientShader`), tagged PDF
> (`SkPDFTag`), graphics state, JPEG / image passthrough, deflate
> compression, and the metadata / UUID helpers.
