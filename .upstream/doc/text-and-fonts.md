# Text & Fonts

> TODO — document the text-and-font stack: `SkFont`, `SkTypeface`,
> `SkFontMgr`, `SkFontStyle`/`SkFontArguments`/`SkFontMetrics`,
> `SkTextBlob`/`SkTextBlobBuilder`, glyph-run plumbing
> (`src/text/GlyphRun.*`, `StrikeForGPU`), the SFNT helpers in `src/sfnt/`,
> and the cross-platform `SkOpenTypeSVGDecoder` hook. Platform-specific
> font hosts are in [Platform Ports](platform-ports.md).
