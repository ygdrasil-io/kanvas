# CanvasKit

> TODO — document `modules/canvaskit/`: the WebAssembly bindings that
> expose Skia, Skottie, SkParagraph, and the debugger to JavaScript —
> `canvaskit_bindings.cpp`, the JS shim layers (`color.js`, `font.js`,
> `cpu.js`, `debug.js`, `bidi.js`, etc.), the npm packaging, the
> WebGL / WebGPU paths, and the HTML-canvas compatibility shim.
