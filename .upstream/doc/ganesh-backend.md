# Ganesh Backend

> TODO — document the legacy GPU backend in `src/gpu/ganesh/` (public
> headers under `include/gpu/ganesh/`): `GrDirectContext` /
> `GrRecordingContext` / `GrContextThreadSafeProxy`, the op architecture
> (`ops/`), draw atoms, render tasks, the clip stack, the `Device`
> implementation, GLSL effects, geometry processors, and the gradient /
> image / surface subsystems.
