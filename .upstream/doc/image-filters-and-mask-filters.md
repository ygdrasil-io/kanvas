# Image Filters & Mask Filters

> TODO — document the `SkImageFilters` DAG (blur, drop shadow, displacement,
> color filter, lighting, magnifier, morphology, offset, picture, runtime,
> shader, tile, blend, merge, etc.) and the `SkMaskFilter` family
> (blur, emboss, table, shader-mask). Implementations live under
> `src/effects/imagefilters/` and `src/effects/colorfilters/`.
