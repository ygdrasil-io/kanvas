# Canvas & Recording API

> TODO — document `SkCanvas` (the heart of the public drawing API),
> the recording variants (`SkPictureRecorder`, `SkPicture`, `SkDrawable`,
> `SkRecorder`), and the wrapper canvases shipped under `include/utils/`
> (`SkNoDrawCanvas`, `SkNWayCanvas`, `SkOverdrawCanvas`,
> `SkPaintFilterCanvas`, `SkNullCanvas`).
