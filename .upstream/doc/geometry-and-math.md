# Geometry & Math

> TODO — document Skia's geometric primitives and math helpers:
> `SkPoint`/`SkPoint3`, `SkRect`/`SkIRect`, `SkRRect`, `SkRegion`,
> `SkPath`/`SkPathBuilder`/`SkPathIter`, `SkMatrix`/`SkM44`,
> `SkVertices`, `SkRSXform`, `SkScalar`, `SkContourMeasure`/`SkPathMeasure`,
> `SkCubicMap`, plus the Bézier helpers in `src/base/SkBezierCurves`
> and `SkCubics`.
