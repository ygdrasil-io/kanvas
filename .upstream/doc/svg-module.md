# SVG Module

> TODO — document `modules/svg/`: SVG parsing, the SVG DOM, attribute /
> style handling, and rendering through SkSG. This is the SVG **input**
> path (parsing and rendering an SVG document); SVG **output** is in
> [SVG Canvas](svg-canvas.md).
