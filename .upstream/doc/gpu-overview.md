# GPU Overview

> TODO — document the concepts shared by both GPU backends in `src/gpu/`:
> atlas types (`AtlasTypes`), blend formulas (`Blend`, `BlendFormula`),
> swizzles, resource keys, async-readback types, tiled-texture utilities,
> dither utilities, blur utilities, buffer writers, and the
> `SkSLToBackend` shim used to drive SkSL through each backend.
