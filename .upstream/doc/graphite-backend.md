# Graphite Backend

> TODO — document the modern GPU backend in `src/gpu/graphite/` (public
> headers under `include/gpu/graphite/`): `Context`, `Recorder` /
> `Recording`, render passes (`render/`), tasks (`task/`), compute
> (`compute/`), the geometry layer (`geom/`), precompiled pipelines
> (`precompile/`), persistent pipeline storage, the clip stack, atlas /
> buffer / proxy management, and the `Device` implementation.
