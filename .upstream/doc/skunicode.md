# SkUnicode

> TODO — document `modules/skunicode/`: Unicode services exposed to the
> rest of Skia (script detection, BiDi resolution, line breaking) and the
> available backends — ICU (full), ICU4X (Rust), libgrapheme, and the
> client / built-in subset.
