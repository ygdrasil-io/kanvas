# Shaders

> TODO — document `SkShader` and its implementations under `src/shaders/`:
> image shader, gradients (linear/radial/sweep/conical, in
> `src/shaders/gradients/`), Perlin noise, picture, color, blend,
> color-filter, coordinate-clamp, gainmap, transform, tri-color, and
> the local-matrix / working-color-space wrappers.
