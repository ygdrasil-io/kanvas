# Testing & Quality

> TODO — document Skia's test surfaces: `dm/` (the "diamond master"
> golden test runner), `tests/` (unit tests via `Test.h`), `gm/` (golden
> master images), `bench/` (microbenchmarks), `fuzz/` (fuzzers and
> regression corpora), plus per-module test directories such as
> `modules/skottie/tests/` and `modules/skparagraph/tests/`.
