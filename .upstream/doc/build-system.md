# Build System

> TODO — document Skia's dual build system: GN (`gn/`, the top-level
> `BUILD.gn`, `.gn`, the per-module `*.gni` files) and Bazel (`bazel/`,
> the per-directory `BUILD.bazel` files, `.bazelrc`, `.bazelversion`),
> plus the cross-cutting build infrastructure — `build_overrides/`,
> `buildtools/`, `toolchain/`, `infra/`, and the `platform_tools/`
> staging area.
