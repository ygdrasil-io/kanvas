# Surface & Output Targets

> TODO — document `SkSurface` and the available output targets: raster
> surfaces, GPU surfaces (Ganesh `SkSurfaces` and Graphite `Surface`),
> `SkRasterHandleAllocator`, and `SkSurfaceProps`. Also covers async
> readback APIs and the relationship between `SkSurface` and `SkImage`.
