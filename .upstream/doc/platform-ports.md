# Platform Ports

> TODO — document `src/ports/` and the corresponding headers in
> `include/ports/`: the OS-glue layer that supplies file I/O
> (`SkOSFile_*`), debug output, discardable memory, font hosts
> (FreeType for Linux/Android, CoreText for macOS/iOS, DirectWrite for
> Windows, Fontations for Rust-based shaping, plus custom-directory /
> embedded / empty managers), and image-generator backends (CG, NDK,
> WIC).
