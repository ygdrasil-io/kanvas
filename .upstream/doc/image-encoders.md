# Image Encoders

> TODO — document the encoders in `src/encode/` and their public headers
> under `include/encode/`: `SkEncoder`, `SkPngEncoder`, `SkPngRustEncoder`,
> `SkJpegEncoder`, `SkWebpEncoder`, plus `SkICC` (ICC profile generation)
> and the JPEG-gainmap encoder.
