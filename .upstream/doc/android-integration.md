# Android Integration

> TODO — document the Android-specific integration surface:
> `include/android/` (`SkAndroidFrameworkUtils`, `SkAnimatedImage`,
> `SkCanvasAndroid`, `SkImageAndroid`, `SkSurfaceAndroid`,
> `GrAHardwareBufferUtils`, `AHardwareBufferUtils`) and `src/android/`,
> plus the framework-perfetto static-storage hook used by AOSP.
