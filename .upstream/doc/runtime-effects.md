# Runtime Effects

> TODO — document `SkRuntimeEffect` (user-authored shaders, color filters,
> and blenders compiled at runtime), the SkSL fragment authored by users,
> uniform/child binding, and how runtime effects are dispatched on each
> backend. The compiler itself is covered in
> [SkSL Shading Language](sksl-shading-language.md).
