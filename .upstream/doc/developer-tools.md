# Developer Tools

> TODO — document the binaries and helpers under `tools/`: Viewer
> (`tools/viewer/`), Fiddle (`tools/fiddle/`), the SkSL compiler /
> minifier (`tools/skslc/`, `tools/sksl-minify/`), `skdiff`, `skpbench`,
> the SKP / SKQP tools, the GPU / Ganesh / Graphite tooling, the Lottie
> performance harness, the calmbench framework, the trace / GDB / SKUI
> helpers, and the platform-specific app shells (`sk_app/`).
