# Vulkan Backend

> TODO — document Vulkan support in both Ganesh (`src/gpu/ganesh/vk/`)
> and Graphite (`src/gpu/graphite/vk/`): `GrVkBackendContext`, the
> Vulkan Memory Allocator integration, command-buffer / queue
> management, external-memory and fence handling, and the
> `MutableTextureState` model.
