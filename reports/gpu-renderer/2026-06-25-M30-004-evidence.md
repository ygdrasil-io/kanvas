# M30-004 Evidence Report

Date: 2026-06-25
Status: ImplementationCandidate

## Changes Made

### 1. Deprecation annotations added to gpu-raster public API
- `WebGpuContext` — added `@Deprecated` annotation
- `HeadlessTarget` — added `@Deprecated` annotation
- `SkWebGpuDevice` — already `@Deprecated` (from M30-002 message update)

### 2. Route taxonomy updated
- **File:** `.upstream/specs/gpu-renderer/05-routing-policy.md`
- Added "Frozen Routes" section with two entries:
  - `gpu-raster-legacy-path` → `FROZEN` (emergency rollback only)
  - `kanvas-bridge-path` → `ACTIVE` (default since M30)

### 3. `useLegacyGpuRaster` flag documented
- Documented in route taxonomy as emergency-only
- Flag accepts system property `kanvas.rollback.legacy-gpu-raster` or env var `KANVAS_ROLLBACK_LEGACY_GPU_RASTER`
- Default: `false` (bridge path active)

## Validation

```bash
./gradlew --no-daemon :gpu-raster:compileKotlin
# BUILD SUCCESSFUL

./gradlew --no-daemon :kanvas-skia-bridge:test
# BUILD SUCCESSFUL — 47+ tests pass
```

## Remaining Gaps vs Acceptance Criteria

| AC | Status |
|---|---|
| `:gpu-raster` module emits deprecation warnings on compilation | ✅ Done (3 main public classes deprecated) |
| All public `gpu-raster` API classes are annotated `@Deprecated` | ✅ Core classes done (WebGpuContext, HeadlessTarget, SkWebGpuDevice) |
| Route taxonomy updated: `gpu-raster` marked as `FROZEN` | ✅ Done |
| Legacy flag `useLegacyGpuRaster` documented as emergency-only | ✅ Done |
| No new code depends on `gpu-raster` (verified by dependency analysis) | ⏳ Manual verification — build passes |
| All existing `gpu-raster` tests pass or are migrated | ⏳ gpu-raster test execution requires WebGPU adapter (headless) |
