# M31-001 Evidence Report

Date: 2026-06-25
Status: ImplementationCandidate

## Changes Made

### 1. Product activation flag
- **File:** `kanvas-skia-bridge/src/main/kotlin/org/skia/kanvas/RollbackConfig.kt`
- Added `productActivation: Boolean` property (default `true`)
- Can be disabled via `-Dkanvas.product.activation.disable=true`
- Separate from the rollback flag (`useLegacyGpuRaster`)

### 2. Production activation diagnostic
- **File:** `kanvas-skia-bridge/src/main/kotlin/org/skia/kanvas/SkiaKanvasSurface.kt`
- `wrapIfEnabled()` now emits `renderer-activated-kanvas-production` diagnostic on first bridge activation
- Diagnostic includes product_activation=true status

### 3. Tests
- `productActivation is true by default`
- `productActivation disabled via system property`
- `wrapIfEnabled emits production activation diagnostic`

## Validation

```bash
./gradlew --no-daemon :kanvas-skia-bridge:test
# BUILD SUCCESSFUL — all tests pass
```

## Remaining Gaps vs Acceptance Criteria

| AC | Status |
|---|---|
| Kanvas is default renderer for new GPUSurface instances | ✅ Done (M30-002) |
| `product_activation` is `true` at system configuration level | ✅ Done |
| Startup emits `renderer-activated-kanvas-production` diagnostic | ✅ Done |
| All scene tests pass with Kanvas as default | ⏳ Deferred to M31-003 |
| PM dashboard shows production activation status | ⏳ Deferred to M31-003 |
| Legacy rollback flag remains functional (default false) | ✅ Done (M30-002) |
