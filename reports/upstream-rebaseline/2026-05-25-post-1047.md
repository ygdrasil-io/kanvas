# Post-1047 upstream rebaseline

Snapshot date: 2026-05-25.

Source baseline: `origin/master` at `c02135d3` (`WebGPU: enforce strict src-rect sampling constraint (#1047)`).

This report supersedes the post-#999 snapshot for active GM planning.
It keeps archived migration phase plans historical only.

## Mechanical artifacts

- Generated TSV: `reports/upstream-rebaseline/2026-05-25-post-1047.tsv`
- Generator: `scripts/gm/upstream-rebaseline.sh`
- GM name hints: `scripts/gm/gm-name-hints.tsv`
- Issue tracker: https://github.com/ygdrasil-io/kanvas/issues/824
- Refresh issue: https://github.com/ygdrasil-io/kanvas/issues/1052

GM summary from the current tree:

| Status | Count |
| --- | ---: |
| `PORTED` | 342 |
| `TEST_DISABLED` | 49 |
| `PARTIAL` | 40 |
| `HELPER` | 4 |
| `STUB` | 1 |
| `MISSING` | 1 |

Mechanical bucket summary:

| Bucket | Count |
| --- | ---: |
| `ported` | 341 |
| `gpu-intractable` | 26 |
| `partial-coverage` | 16 |
| `codec-gated` | 14 |
| `font-gated` | 9 |
| `slow-or-reference` | 6 |
| `implementation` | 5 |
| `helper` | 4 |
| `fixture-gated` | 4 |
| `alias` | 4 |
| `wgsl-runtime-gated` | 3 |
| `partial-untagged` | 3 |
| `platform-gated` | 1 |
| `build-example` | 1 |

## Mapping cleanup

The generator now consumes explicit GM name hints for Kotlin ports whose
registered names are computed from constructor state, differ only by
C++/Kotlin spelling, or collapse several upstream registrations into one
parameterized Kotlin file.

Rows retired from false `missing-mapping`:

| Row | New classification |
| --- | --- |
| `aarecteffect` | `TEST_DISABLED`, `gpu-intractable` |
| `bigrrectaaeffect` | `TEST_DISABLED`, `gpu-intractable` |
| `coloremoji_blendmodes` | `TEST_DISABLED`, `font-gated` |
| `constcolorprocessor` | `TEST_DISABLED`, `gpu-intractable` |
| `crbug_224618` | `PORTED`, `ported` |
| `encode_srgb` | `TEST_DISABLED`, `codec-gated` |
| `fontations` | `TEST_DISABLED`, `font-gated` |
| `glyph_pos` | `PORTED`, `ported` |
| `hdr_pip_blur` | `PORTED`, `ported` |
| `imagefromyuvtextures` | `TEST_DISABLED`, `codec-gated` |
| `kawase_blur_rt` | `PORTED`, `ported` |
| `linepaths` | `PORTED`, `ported` |
| `scaledemoji_rendering` | `TEST_DISABLED`, `font-gated` |
| `strokerect_anisotropic` | `PORTED`, `ported` |
| `texelsubset` | `TEST_DISABLED`, `gpu-intractable` |
| `yuvtorgbsubset` | `TEST_DISABLED`, `codec-gated` |

The only remaining mechanical `MISSING` row is `hello_bazel_world`,
classified as `build-example` rather than actionable GM implementation work.

## Delivered since post-999

The post-#999 actionable batch is reflected in this snapshot:

- `colrv1` is now `PORTED`.
- `palette` is now `PORTED`.
- `asyncrescaleandread` remains `codec-gated` for YUV/media parity, but the
  RGBA CPU fallback is no longer an implementation blocker.
- The #1007 `vertices` texOnly/scaled-gradient slice has landed, but the
  broader `vertices` row remains an `implementation` bucket in the TSV for
  remaining visual-parity work.
- `custommesh`, bounded LCH powerless hue, YUVA metadata helpers, and WebGPU
  strict source-rect sampling are no longer listed as current actionable
  backlog items in this report.

## Current gated rows

Font-gated rows:

- `coloremoji`
- `coloremoji_blendmodes`
- `dftext`
- `fontations`
- `fontations_ft_compare`
- `pdf_never_embed`
- `scaledemoji`
- `scaledemoji_rendering`
- `stroketext`

Codec/media-gated rows:

- `animated_gif`
- `animated_image_orientation`
- `asyncrescaleandread` for YUV/media variants
- `bc1_transparency`
- `compressed_textures`
- `ducky_yuv_blend`
- `encode_srgb` for WebP encoder/fixture parity
- `exoticformats`
- `imagefromyuvtextures`
- `rendertomipmappedyuvimageplanes`
- `video_decoder`
- `wacky_yuv_formats`
- `yuv420_odd_dim`
- `yuvtorgbsubset`

WGSL/parser-gated rows remain blocked on WebGPU shader parser or typed WGSL
runtime-effect registrations:

- `rippleshadergm`
- `runtimeshader`
- `workingspace`

Out-of-scope GPU backend rows stay out of the CPU/WebGPU convergence plan:

- Ganesh/Graphite-specific rows such as `aarecteffect`,
  `bigrrectaaeffect`, `constcolorprocessor`, `gpu_blur_utils`,
  `graphitestart`, `slug`, `texelsubset`, and private GPU YCbCr paths.

## Tracker updates

After this report lands, update issue `#824` to point at this snapshot and
the `2026-05-25-post-1047.tsv` artifact. The broad post-#999 implementation
batch is complete; future tickets should be opened only from observed
non-gated rows or explicit dependency deliveries.
