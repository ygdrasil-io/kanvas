# Post-1087 upstream rebaseline

Snapshot date: 2026-05-25.

Source baseline: `origin/master` after #1084, #1085, and #1086.

## Mechanical artifacts

- Generated TSV: `reports/upstream-rebaseline/2026-05-25-post-1087.tsv`
- Generator: `scripts/gm/upstream-rebaseline.sh`
- Issue tracker: https://github.com/ygdrasil-io/kanvas/issues/1087

GM summary from this branch:

| Status | Count |
| --- | ---: |
| `PORTED` | 351 |
| `TEST_DISABLED` | 44 |
| `PARTIAL` | 36 |
| `HELPER` | 4 |
| `STUB` | 1 |
| `MISSING` | 1 |

Mechanical GM progress is **80.3%**: 351 `PORTED` rows out of 437
tracked rows.

Actionable convergence is **87.3%**: 351 `PORTED` rows out of 402
actionable rows after excluding `helper`, `build-example`,
`gpu-intractable`, `wgsl-runtime-gated`, and `platform-gated` buckets.

## Classification hygiene

- `partial-untagged` is now **0**.
- `lumafilter`, `shadowutils`, and `surface` are classified as
  `partial-coverage`, matching the post-#1080 audit evidence.
- `gradients` is classified as `implementation`; its remaining row-level
  blocker is gradient interpolation fidelity, not a missing dependency.
- `drawatlas` remains mechanically `PORTED`.
- `mesh` remains the only `implementation` row besides `gradients`, now
  narrowed by #1085 and #1086 to remaining child-shader / GPU-zero-init /
  fixture parity surfaces.

No Ganesh or Graphite work is in scope for this snapshot.
