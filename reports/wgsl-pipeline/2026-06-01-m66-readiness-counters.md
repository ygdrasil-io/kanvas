# M66 readiness counters

Date: 2026-06-01
Linear: FOR-42 preparation under FOR-32

## Prepared counters

M66 prepares the counted evidence needed for parent closeout without editing
`README.md` or `.upstream/target/*`.

| Area | Before M66 | M66 evidence | Prepared after M66 |
|---|---:|---:|---:|
| Selected GM/reference evidence rows | 31/100 | +19 selected support/refusal rows with `referenceKind` | 50/100 prepared |
| Skia-comparable fidelity rows | 31/100 | +6 `skia-upstream` rows; test/cpu oracles require parent review before fidelity credit | 37/100 minimum prepared |
| PM/demo operability artifacts | 11/20 | +2 reports plus PM bundle M66 manifest entry | 13/20 prepared |
| Rendering feature breadth | 6/10 | no new family denominator; rows normalize existing families | 6/10 unchanged |
| Performance/cache readiness | 7/20 | no new release-blocking gate | 7/20 unchanged |
| Real-time runtime | 1/10 | no runtime capability change | 1/10 unchanged |

## Reference-kind split

| referenceKind | Rows | Score implication |
|---|---:|---|
| `skia-upstream` | 6 | Counts toward Skia-like fidelity when artifacts/routes/stats pass gates. |
| `test-oracle` | 6 | Counts as selected generated evidence; Skia fidelity only when the oracle is Skia-derived. |
| `cpu-oracle` | 7 | Counts toward breadth/refusal visibility, not Skia fidelity unless comparable Skia reference lands later. |

## Closeout decision

The final closeout credits M66 with the selected evidence movement from 31/100
to 50/100. The Skia-comparable minimum is tracked separately as 37/100 because
only the 6 new `skia-upstream` rows are direct comparable additions in this
wave. CPU-oracle rows remain breadth/refusal evidence until comparable Skia
references land.

The active target readiness moves to approximately 53% only when combined with
M65 runtime smoke evidence. The material M66 evidence is generated by:

```bash
rtk ./gradlew --no-daemon pipelineM66GmPromotionWave pipelineSceneDashboardGate pipelinePmBundle
```

No threshold was lowered globally. No font, codec, or arbitrary SkSL substitute
was introduced.
