# M51 PM Report: Skia GM Inventory Coverage

Date: 2026-05-31
Verified commit: `2da25efa`
Milestone: M51

## Executive Summary

M51 is treated and verified. It does not make every Skia sample supported; it
makes the full Skia GM/sample surface visible and actionable. The PM bundle now
contains inventory artifacts, inventory gate output, mismatch counters, and the
next promotion backlog.

Post-MVP readiness moves from 80% to 82%. The movement is intentionally small
because inventory is planning evidence, not rendered support evidence.

## What Changed

- Added `scripts/skia_gm_inventory.py`.
- Added Gradle tasks:
  - `pipelineSkiaGmInventory`;
  - `pipelineSkiaGmInventoryGate`.
- Added inventory and gate artifacts to `pipelinePmBundle`.
- Added M51 sprint review and updated README/target/backlog score to 82%.

## Verified Counters

| Signal | Count |
|---|---:|
| Upstream GM files | 437 |
| Kotlin GM files | 751 |
| Inventory rows | 802 |
| Classified rows | 183 |
| Not-triaged rows | 619 |
| Dashboard-promoted rows | 17 |
| Promotion-candidate rows | 35 |
| M52+ backlog candidates | 34 |
| Dependency-gated rows | 38 |
| Expected-unsupported rows | 29 |
| Duplicate/variant rows | 60 |
| Non-rendering/utility rows | 4 |
| Matched upstream/Kotlin rows | 386 |
| Upstream-only rows | 51 |
| Kotlin-only rows | 365 |

The existing scene dashboard remains unchanged as support evidence:

| Signal | Count |
|---|---:|
| Scene rows | 28 |
| `pass` | 21 |
| `expected-unsupported` | 7 |
| `tracked-gap` | 0 |
| `fail` | 0 |
| Generated evidence rows | 26 |
| Static policy rows | 2 |
| Adapter-backed rows | 17 |

## PM Bundle Evidence

`pipelinePmBundle` now includes:

- `inventory/inventory.json`;
- `inventory/inventory.md`;
- `inventory-gate/inventory-gate.md`;
- `inventoryCounters` in `manifest.json`;
- `dashboardInventoryLinks`;
- a clear inventory notice: inventory rows classify planning state only and do
  not change dashboard support claims or counters.

## Validation

```bash
rtk git diff --check
rtk ./gradlew --no-daemon pipelineSkiaGmInventory pipelineSkiaGmInventoryGate pipelineSceneDashboard pipelineSceneDashboardGate pipelinePmBundle
```

Result: pass.

## Score Decision

M51 earns 82% readiness:

| Area | M50 | M51 | Reason |
|---|---:|---:|---|
| Evidence foundation | 100% | 100% | Existing dashboard gate remains green and deterministic. |
| Skia integration coverage | 65% | 70% | Full GM inventory is visible, classified, mismatch-reported, and linked to M52+ candidates. |
| CI and release gates | 85% | 85% | Inventory gate is additive and does not weaken the M50 release gate. |
| Performance readiness | 60% | 60% | No performance threshold change. |
| PM demo and reporting workflow | 85% | 88% | PM bundle now carries inventory, inventory gate output, counters, links, and limitations. |

## Limits

- M51 does not claim broad Skia GM support.
- M51 does not add hundreds of dashboard support rows.
- M51 does not clear the 619 not-triaged rows.
- M51 does not clear dependency-gated font, codec, emoji, shaping, SDF, LCD, or
  glyph-mask gaps.
- M51 does not turn performance warnings into release-blocking thresholds.

## Recommended Next Step

M52 should promote a small selected pack from the M51 candidate backlog into
generated scene evidence. The likely target is 8-12 candidates across
paint/blend, bitmap/image, gradients, clip/transform, Path AA, image filters,
runtime effects, text/font, and codec/image-decode boundaries. The sprint should
keep inventory status separate from rendered support claims.
