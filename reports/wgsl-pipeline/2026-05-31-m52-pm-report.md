# M52 PM Report: GM Inventory Promotion Pack

Date: 2026-05-31
Milestone: M52
Linear epic: GRA-302

## Executive Summary

M52 is treated and verified. It promotes 10 selected M51 GM inventory candidates
into generated dashboard evidence while keeping inventory status separate from
rendering support claims.

Final dashboard state: 38 rows, 28 pass, 10 expected-unsupported, 0
tracked-gap, 0 fail, 36 generated rows, 2 static policy rows, 24
adapter-backed rows, and 10 inventory-derived generated rows.

Post-MVP readiness moves from 82% to 85%. The movement is limited because M52
adds selected evidence, not broad Skia GM parity.

## PM Bundle Evidence

`pipelinePmBundle` now includes:

- generated dashboard rows and artifacts for the 10 M52 promotions;
- `m52InventoryPromotion` in `manifest.json`;
- selected/promoted/rejected row counters;
- promoted row detail with `sceneId`, `inventoryId`, status, and source report;
- limitations that inventory rows remain planning evidence unless promoted into
  generated dashboard evidence.

## Verified Counters

| Signal | Count |
|---|---:|
| Scene rows | 38 |
| `pass` | 28 |
| `expected-unsupported` | 10 |
| `tracked-gap` | 0 |
| `fail` | 0 |
| Generated evidence rows | 36 |
| Static policy rows | 2 |
| Adapter-backed rows | 24 |
| Inventory-derived generated rows | 10 |
| M52 selected candidates | 10 |
| M52 promoted candidates | 10 |
| M52 rejected/deferred candidates documented | 8 |

## Score Decision

M52 earns 85% readiness:

| Area | M51 | M52 | Reason |
|---|---:|---:|---|
| Evidence foundation | 100% | 100% | Existing dashboard generation and gate invariants remain green. |
| Skia integration coverage | 70% | 82% | 10 selected M51 candidates now have generated evidence or stable generated refusal rows. |
| CI and release gates | 85% | 85% | Inventory-derived row gates are additive and do not weaken existing gates. |
| Performance readiness | 60% | 60% | Performance thresholds remain warning-only. |
| PM demo and reporting workflow | 88% | 90% | PM bundle now exposes selected/promoted/rejected M52 counters and limitations. |

Weighted readiness: 85%.

## Validation

```bash
rtk git diff --check
rtk ./gradlew --no-daemon pipelineSkiaGmInventory pipelineSkiaGmInventoryGate
rtk ./gradlew --no-daemon pipelineSceneDashboard pipelineSceneDashboardGate
rtk ./gradlew --no-daemon pipelinePmBundle
```

Result: pass.

## Limits

- No broad Skia GM support is claimed.
- M51 inventory rows remain planning evidence unless promoted.
- Expected-unsupported rows remain visible and stable.
- Dependency-gated font, codec, emoji, shaping, SDF, LCD, glyph-mask, arbitrary
  image-filter DAG, arbitrary SkSL, and broad Path AA gaps remain open.
