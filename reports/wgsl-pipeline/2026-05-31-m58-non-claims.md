# M58 Non-Claims

Result: pass.

M58 is a performance release gate only for selected measured rows.

## Non-Claims

- No rendering support expansion is claimed.
- No broad Skia GM parity is claimed.
- No broad Path AA, arbitrary image-filter DAG, font, emoji, shaping, codec,
  arbitrary SkSL, or Graphite/Ganesh scope is claimed.
- Estimated metrics are not promoted to measured.
- Missing metrics are not treated as passing measured evidence.
- Deferred rows remain visible and cannot move score by themselves.
- The release gate does not execute new GPU captures; it validates dashboard
  performance payloads and explicit M58 thresholds.

## Active Claim

The only new M58 claim is that
`rtk ./gradlew --no-daemon pipelinePerformanceReleaseGate` blocks release when a
selected measured lane exceeds its explicit threshold or lacks required measured
metadata.
