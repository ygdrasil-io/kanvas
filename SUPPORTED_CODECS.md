# Supported Codecs

This matrix is the official support contract for the portable codec and image
encode paths.

Validation command:

```bash
./gradlew checkCodecImageComplete
```

JPEG classic release command:

```bash
./gradlew checkJpegClassicComplete
```

`checkCodecImageComplete` runs the pure Kotlin codec tests, real image tests, runtime assembly
checks, image encode tests, image encode guard checks, production classpath
checks, and AWT/ImageIO guards. Format-specific rows below call out limitations
that are intentional for the current implementation, either as temporary roadmap
gaps or as out-of-scope behavior.

## Decode Matrix

| Format | Decode | Animation | ICC / color metadata | EXIF / orientation | Alpha | 16 bpc / F16 | Progressive / interlace | Unsupported variants and notes |
|---|---|---|---|---|---|---|---|---|
| PNG | Supported through `codec/png`. Covers grayscale, RGB, indexed, grayscale+alpha, and RGBA PNGs. | Static PNG only; APNG (`acTL`, `fcTL`, `fdAT`) is explicitly refused. | Only `PngMetadataValue.Resolved` color chunks participate, in `cICP > iCCP > sRGB > cHRM+gAMA` order. `getICCProfile()` exposes only a valid embedded `iCCP`; resolved cICP, sRGB, and cHRM+gAMA states do not fabricate ICC provenance. Full-range RGB `cICP` resolves normally; narrow-range RGB `cICP` is explicit `png.cicp.narrow-range.unsupported` and samples stay untransformed. Grayscale `cICP` is info-only. `gAMA` alone neither resolves an sRGB metadata profile nor fabricates ICC provenance; untagged decoding uses the normal sRGB default. Valid `cHRM+gAMA` derives a matrix/TRC profile. Refused color metadata remains diagnostic-only. `getPixels` preserves samples and refuses unimplemented scale, alpha, and color-space transforms. | Not applicable. | Supported through alpha channels and `tRNS` for grayscale, RGB, and indexed PNGs. Indexed samples outside `PLTE` decode as opaque black. | 16-bit grayscale/RGB/grayscale+alpha/RGBA decode to `RGBA_F16Norm`. | Non-interlaced and Adam7 interlaced PNGs are supported. | `PngDocument` preserves pristine static PNG bytes exactly. Ancillary-only metadata edits retain untouched raw chunks, including fragmented `IDAT`; pixel or critical-impact changes are explicitly refused until re-encoding is implemented. Critical unknown chunks, invalid CRCs, malformed container structure, truncated or trailing zlib `IDAT` data, and unsupported color/depth combinations are rejected. Refused semantic color metadata is not applied. |
| JPEG | Supported through `codec/jpeg` for sequential Huffman DCT SOF0/SOF1 (8/12-bit), progressive Huffman DCT SOF2 (8/12-bit), sequential/progressive arithmetic DCT SOF9/SOF10 (including verified sequential 12-bit), Huffman lossless SOF3 (8/12/16-bit), and verified DHP/EXP hierarchy composition. Grayscale, YCbCr, RGB, CMYK and YCCK routes are selected from valid frame/metadata combinations; sampling factors 1..4 are covered. | Not applicable. | Multi-segment APP2 ICC profiles are parsed and exposed when complete and parseable. | EXIF orientation 1 through 8 is parsed and applied to output dimensions/pixels. | JPEG has no alpha channel. Output is opaque RGBA. | Decode can write `RGBA_F16Norm`, normalized from the source 8/12/16-bit precision supported by the decoded process. | Interleaved multi-component progressive scans, refinement, restart intervals and arithmetic DCT scans are supported by the tested routes. | Bare differential SOFs require hierarchy reference state and are explicitly refused; SOF11 arithmetic lossless is explicitly unsupported. Malformed APP markers, incomplete ICC segment sets, illegal scan scripts, invalid entropy/restart state, unsupported frame/color combinations and malformed hierarchy references are rejected or return `kUnimplemented`. |
| JPEG-LS | Supported through `codec/jpeg-ls` for SOF55 8-bit grayscale LOCO-I and RGB scans with component IDs 1/2/3 and unit sampling. Grayscale uses `ILV=0`; RGB supports three ordered `ILV=0` non-interleaved scans with `NEAR=0`, `ILV=1` (line interleave) with `NEAR=0..127`, and `ILV=2` (sample interleave) with `NEAR=0`, including proven run/run-interruption coding. The decoder accepts implicit/default or validated custom LSE parameters for `MAXVAL=255`, and regular/run/run-interruption coding. | Not applicable. | APP and COM segments are retained by the immutable document, but ICC color management is not exposed in this profile. APP8 `mrfx` with transform 0 is retained; HP1/HP2/HP3 transforms are explicitly refused. | Not applicable. | JPEG-LS profile has no alpha in this implementation. Output is opaque RGBA. | Not applicable; output is normalized from 8-bit samples. | Not applicable. | LSE with another `MAXVAL`, invalid LSE bounds, non-lossless RGB `ILV=0` or `ILV=2`, non-RGB component IDs/sampling/mapping tables, DRI/RST, point transforms, nonzero APP8 `mrfx` colour transforms and other JPEG-LS profiles return stable diagnostics; `NEAR>127` is rejected for the 8-bit `MAXVAL=255` profile. Malformed SOF55/SOS/entropy/EOI streams are refused by JPEG-LS without fallback to JPEG classic. |
| JPEG 2000 (J2K / JP2) | `codec/jpeg2000` retains a bounded Part-1 structural foundation for general `SIZ`/`COD`/`QCD` main headers and `SOT` tile plans, without widening the public pixel facade. Public pixels remain exclusively the proven pure-Kotlin raw J2K profile and the JP2 facade whose embedded codestream satisfies that same bounded lossless profile: one unsigned 8-bit grayscale component, one tile, LRCP, one layer/precinct, reversible QCD and style-0 Tier-1 EBCOT. `Ndecomp=0` supports one packet with one codeblock (`1..64` pixels wide) or two horizontal codeblocks (`65..128`), height `1..64`. `Ndecomp=1` supports the bounded two-packet sequence (LL, then HL/LH/HH); `Ndecomp=2` supports the bounded three-packet sequence (LL2, then HL2/LH2/HH2, then HL1/LH1/HH1). Both multi-resolution profiles use one 64×64-or-smaller codeblock per subband, dimensions `2..128` × `2..64` for `Ndecomp=1` or `4..128` × `4..64` for `Ndecomp=2`, the reversible integer 5/3 inverse DWT (transformée en ondelettes inverse), and level shift into opaque `RGBA_8888`. The pixel facade rejects larger dimensions before allocation. A JP2 `jp2h` pixel facade permits only its matching one-component `ihdr` and no `colr` or one exact enumerated grayscale `colr` declaration (method 1, precedence 0, approximation 0, enum 17). | Static only; no frame/animation decode. | Well-formed ICC, non-grayscale and multiple `colr` declarations are retained structurally but never decoded or exposed as color/ICC pixels; each disables the JP2 pixel facade. Malformed `colr` remains a structural input error. | Not applicable. | No alpha decode; output is opaque `RGBA_8888`. | The proven raw and JP2 profile is unsigned 8-bit grayscale only. | Only LRCP with one layer/precinct and `Ndecomp=0..2` as bounded above is decoded; no general multi-level inverse DWT claim is made. | There is no J2K/JP2 pixel fallback to OpenJPEG, AWT, ImageIO, JNI or another codec. Until independently verified follow-on work lands, the pixel facade explicitly refuses or leaves unimplemented multi-component or multi-tile images, every progression other than LRCP, irreversible 9/7, MCT, JP2 color/ICC outside the one exact grayscale declaration, JPX, MJ2, HTJ2K, JP2 palette (`pclr`), component mapping (`cmap`), channel definition (`cdef`, including alpha), PPM/PPT/SOP/EPH, vertical or more-than-two codeblocks per subband, deeper resolutions, quantized or non-reversible profiles, region APIs, metadata rewriting, and every encoder route. Pinned OpenJPEG fixtures, including the JP2 5×5 and `Ndecomp=2` fixtures, are checked pixel-for-pixel; the optional OpenJPEG executable remains validation evidence only. |
| GIF | Supported through `codec/gif`. Covers GIF87a/GIF89a indexed frames. | Supported for decoded frame count, frame rects, frame durations, transparency, interlaced frames, disposal modes used by the current tests, and Netscape loop count metadata exposed through `Codec.getRepetitionCount()`. | GIF ICC/color profiles are not exposed. Output uses sRGB. | Not applicable. | Supported through transparent color indexes. | Not applicable. Output is `RGBA_8888`. | GIF interlacing is supported. | Malformed LZW streams, invalid frame bounds, missing color tables, zero-sized canvases, and unsupported extension semantics are rejected. Malformed loop extension payloads are skipped unless they corrupt frame data. |
| BMP | Supported through `codec/bmp`. Covers Windows/OS2 `BM` files with BITMAPINFOHEADER-compatible DIBs. | Not applicable. | V4/V5 embedded ICC profiles are parsed and exposed when parseable. Output uses sRGB. | Not applicable. | 32 bpp alpha masks are supported when present; otherwise pixels are opaque. | Not applicable. Output is `RGBA_8888`. | Not applicable. | BI_RGB, BI_BITFIELDS, BI_RLE8, and BI_RLE4 are supported for 1/4/8/16/24/32 bpp where valid. Top-down RLE is rejected. Unsupported compression modes, invalid masks, malformed palettes, and truncated pixel data are rejected. |
| WBMP | Supported through `codec/wbmp`. Covers WAP type-0 monochrome WBMP. | Not applicable. | Not applicable. Output uses sRGB. | Not applicable. | Not supported by the format. Output is opaque black/white RGBA. | Not applicable. Output is `RGBA_8888`. | Not applicable. | Non-type-0 headers, zero dimensions, VLQ overflow, excessive dimensions, and truncated pixel data are rejected. |
| ICO / CUR | Supported through `codec/ico` as a container decoder. It selects the largest entry and delegates PNG payloads to PNG decode or DIB payloads to BMP decode. | Multi-entry selection is supported; ICO is not exposed as animation. | Follows the selected payload decoder. | Follows the selected payload decoder. | PNG alpha, BMP alpha masks, and supported ICO AND masks are applied through the selected payload path. | Follows the selected payload decoder. | Follows the selected payload decoder. | Dimensions encoded as 0 are treated as 256. Invalid directories, truncated entries, unsupported payloads, and undecodable selected payloads are rejected. |
| WebP | Supported through `codec/webp` for container metadata and the implemented pixel paths. VP8L lossless and a VP8 lossy keyframe subset are covered by real-image tests. | Animated WebP metadata, ANIM loop count exposed through `Codec.getRepetitionCount()`, and frame composition are implemented when each frame uses a supported pixel encoding. Unsupported frame encodings return `kUnimplemented`. | VP8X ICC chunks are parsed and exposed when parseable. EXIF and XMP chunks are preserved internally as metadata. Output color space remains sRGB. | EXIF bytes are parsed as metadata only; EXIF orientation is not applied. | VP8L alpha is supported. VP8 alpha metadata is parsed; unsupported alpha compression/features return `kUnimplemented`. | Not applicable. Output is `RGBA_8888`. | Not applicable. | VP8 lossy features outside the implemented subset return `kUnimplemented`. Truncated RIFF/VP8X/VP8L/VP8 streams, invalid dimensions, inconsistent flags, and unsupported animation frame encodings are rejected or return `kUnimplemented`. |
| JPEG XL | `codec/jpegxl` decodes one proven pure-Kotlin pixel profile: raw `FF 0A`, an exact `JXL `/`ftyp`/`jxlc` envelope, or an ordered version-0 `jxlp` fragment sequence. The decoded image is static RGB or grayscale 8-bit lossless Modular with direct samples, sRGB/D65 color encoding, no alpha, palette, transforms, animation, or container metadata. All samples are range-checked and returned as opaque `RGBA_8888`. Pinned libjxl grayscale raw/`jxlc` fixtures plus compact RGB raw/`jxlc`/`jxlp` regression pixels are compared exactly with their sources. Other raw JXL variants and container topologies return explicit `kUnimplemented`. | Animation is not decoded. | ICC/color metadata is not decoded outside the direct sRGB/grayscale profile. | EXIF/orientation is not decoded. | Alpha is not decoded. | HDR/F16 is not decoded. | Not applicable. | There is no JPEG XL encoder or JPEG reconstruction path. Out-of-order version-1 `jxlp`, extra/metadata boxes, VarDCT, palette/squeeze/RCT transforms, weighted/reference MA trees, and every other container topology are explicitly `kUnimplemented`; no JXL input may silently fall back to AWT/ImageIO/JNI or another codec. |
| AVIF / RAW / video | Not supported in the portable pure Kotlin runtime. `codec/extended` reserves provider stubs for explicit future dependency deliveries. | Out of scope. | Out of scope. | Out of scope. | Out of scope. | Out of scope. | Out of scope. | These formats must not silently fall back to AWT/ImageIO/JNI in production portable paths. |

## Encode Matrix

| Format | Encode | ICC / color metadata | EXIF / orientation | Alpha | 16 bpc / F16 | Progressive / interlace | Unsupported variants and notes |
|---|---|---|---|---|---|---|---|
| PNG | Supported through `PngEncoder`. | The encoder writes `sRGB` (rendering intent 0) and `gAMA` (45455) chunks for sRGB source images. Non-sRGB `iCCP` encode is limited to supported RGB SDR matrix/TRC color spaces that can be serialized as ICC; HDR, LUT-based, unsupported, and otherwise non-serializable color spaces are refused before output (`null`/`false`). `cHRM` is not serialized. | Not applicable. | Supported. | F16 and 16 bpc preservation are not official support; input pixels are projected through the current 8-bit pixmap/bitmap path. | Adam7 interlaced PNG encode is supported with per-pass filter selection. | Compression and filters are pure Kotlin and covered by existing encode tests. Real-corpus quality/conformance expansion remains a roadmap item. |
| JPEG | Supported through `JpegEncoder` in `codec/jpeg/` for sequential Huffman RGB output (SOF0 8-bit and SOF1 12-bit), sequential arithmetic grayscale/YCbCr output (SOF9 8/12-bit with explicit default DAC), progressive Huffman and arithmetic grayscale/YCbCr scan scripts (SOF2/SOF10 8/12-bit), Huffman lossless grayscale/RGB output, and four narrow two-level hierarchies: 8-bit grayscale S444 SOF0 plus differential Huffman SOF5 with ×2 expansion; SOF9 plus differential arithmetic SOF13 with ×2 expansion; initial DC+AC SOF10 plus differential arithmetic SOF14 with ×2 expansion; or same-size SOF0 plus differential Huffman lossless SOF7 with `EXP=00`. Quality is `0..100`, sampling factors are `1..4` for DCT, and DRI/RST is optional. | `JpegEncodeMetadata` writes caller-supplied ICC APP2 chunks (or serializable non-sRGB source ICC), Adobe APP14 transform 1 (YCbCr) or 0 (lossless RGB), optional EXIF APP1, XMP APP1 and COM. | Legacy `Options.orientation` writes EXIF 1–8; typed EXIF TIFF bytes take precedence when supplied. | JPEG output is opaque; `JpegAlphaPolicy` explicitly ignores alpha or composites it on black. | 12-bit DCT and 8/12/16-bit lossless codestreams are supported from 8-bit bitmap input by precision expansion; F16 source preservation is not supported. | Progressive Huffman and arithmetic require an explicit initial-only scan script (`Ah = Al = 0`); the SOF10→SOF14 hierarchy further fixes that script to grayscale DC then AC; lossless Huffman requires explicit predictor and point transform. | SOF11 remains refused. Differential/hierarchy encode is refused outside the documented SOF0→EXP×2→SOF5, SOF9→EXP×2→SOF13, initial-DC+AC SOF10→EXP×2→SOF14, or SOF0→EXP00→SOF7 grayscale S444 intersections; SOF9/SOF10 are independently checked by opt-in `djpeg` oracles, while hierarchy intersections are checked by the opt-in ISO/IEC 10918-7 reference oracle. Optimized Huffman table selection is intentionally not exposed. |
| JPEG-LS | Supported through `JpegLsEncoder` for grayscale 8-bit SOF55 and RGB `ILV=1` (line) with `NEAR=0..127`, plus RGB `ILV=0` (three non-interleaved scans) and `ILV=2` (sample) with `NEAR=0`. It emits implicit defaults or a validated explicit LSE for `MAXVAL=255`. | APP/COM emission is not exposed by the initial encoder API. | Not applicable. | The encoder requires fully opaque input and emits opaque output; any translucent pixel is refused (`null`). | Input dimensions must be within `1..65535`. Pixel-wise grayscale inputs emit grayscale; other opaque pixels emit RGB components exactly, without a colour transform. | Not applicable. | Non-lossless `ILV=0`/`ILV=2`, invalid custom LSE bounds, DRI/RST and point transforms are refused until independently evidenced. |
| JPEG 2000 (J2K / JP2) | No portable encoder is exposed. | Not applicable. | Not applicable. | Not applicable. | Not applicable. | Not applicable. | J2K/JP2 encoding must not delegate to OpenJPEG or any native/JNI/AWT/ImageIO path. A future encoder requires deterministic Tier-1 EBCOT/MQ, packet and tile-part construction, plus independent fixture and oracle evidence before this row can be promoted. |
| BMP | Supported through `BmpEncoder`. Implemented in `codec/bmp/`. | V5 embedded ICC profiles are written when supplied via `Options.iccProfile`. Output defaults to sRGB. | Not applicable. | Supported when writing 32 bpp output; otherwise format variants are opaque. | Not supported. | Not applicable. | Supports uncompressed BGRA_8888 and BGR_888, plus RLE8/RLE4 compression for palette-indexable input. |
| WBMP | Supported through `WbmpEncoder` in `codec/wbmp/`. | Not applicable. | Not applicable. | Not supported by the format. Pixels are encoded as 1-bit black/white. | Not applicable. | Not applicable. | Output is WAP type-0 monochrome WBMP. |
| WebP | Supported through `WebpEncoder` for lossless VP8L with VP8X extended format when metadata is present. | ICCP (ICC profile), EXIF, and XMP chunks are written as VP8X sub-chunks via `Options.iccProfile`, `Options.exifData`, and `Options.xmpData`. | EXIF orientation writing is not supported. | Supported for lossless VP8L. | Not supported; input pixels are projected to 8-bit samples. | Not applicable. | Lossy VP8 encode intentionally returns `null` until a pure Kotlin lossy encoder is implemented or explicitly kept out of scope. |
| ICO / CUR | Supported through `IcoEncoder` in `codec/ico/`. Encodes multi-image ICO with PNG (via `PngEncoder`) or BMP (via `BmpEncoder`) payload per entry. Single-image encode is preserved as backward-compatible shorthand. | Encoder writes per-payload metadata (PNG sRGB/gAMA/iCCP or BMP V5 ICC) through the delegated format encoder; no additional ICC metadata is serialized on the ICO envelope. | Not applicable. | Alpha supported through the embedded payload (PNG alpha or BMP alpha mask). | Not supported. | Not applicable. | CUR (cursor) encode is not supported. Public encode API returns `null` for degenerate bitmaps or empty entry lists. |
| GIF | Supported through `GifEncoder` in `codec/gif/` for single-frame and multi-frame indexed GIF89a output with LZW compression, per-frame Graphic Control Extensions (disposal, delay), and Netscape loop extension. | Not applicable. GIF output uses the global color table and is indexed. | Not applicable. | Not supported by the encode path; output is opaque indexed. | Not applicable. Output is indexed with up to 256 colors. | Not applicable. | Per-frame local color tables are not supported; all frames share a single global palette. Encode API returns `null` for degenerate bitmaps. |
| AVIF / HEIF / JPEG XL / RAW / video | Not supported by the public portable encoder path. | Out of scope. | Out of scope. | Out of scope. | Out of scope. | Out of scope. | Public encode APIs must return `null` or a documented stub behavior for unsupported formats; they must not use AWT/ImageIO/JNI fallbacks. |

## Guardrails

- Production portable codec runtime is `:codec`.
- The runtime and image encode production paths must not depend on AWT,
  ImageIO, or JNI codec backends.
- Unsupported variants should fail explicitly by returning `null`,
  `kUnimplemented`, or a specific `Codec.Result` error rather than silently
  selecting another backend.
- New support claims should land with tests in the owning codec module or
  `codec/real-image-tests`, then this matrix should be updated in the same PR.

### JPEG release evidence

`checkJpegClassicComplete` runs the Kotlin-only `:codec:jpeg:test` suite, the
separate non-cacheable `:codec:jpeg:jpegPerformanceEvidence` task, and the
codec support, fixture-provenance, production and test AWT/ImageIO guardrails.
Its
`JpegConformanceTest` enumerates every non-reserved JPEG SOF as either an
evidenced decode/encode route or an asserted stable refusal. A bare SOF6 is
refused because it has no reference state, while the pinned DHP/EXP SOF6 corpus
is separately exercised as a valid hierarchy route. `JpegFuzzTest`
deterministically mutates marker lengths, entropy, segment order, DRI/DAC and
truncation while requiring bounded document handling and a bitmap or stable
diagnostic result from every opened document. `jpegPerformanceEvidence` has no
timing threshold: it deletes any previous report, runs the dedicated test, and
writes warm-up plus ten-decode median evidence to
`codec/jpeg/build/reports/jpeg-performance.json` for photo, graphic, CMYK,
12-bit, and large-image fixture classes.

`JpegOracleTest` is opt-in only. Supplying
`-PjpegOracleDir=/absolute/path/to/directory-containing-djpeg` enables its
external SOF9/SOF10 parse check; no executable is searched through `PATH`, and
neither the external binary nor a native codec is a runtime or normal-CI
dependency. The stronger pixel-comparison oracle commands and their pinned
fixture provenance remain documented beside the arithmetic and hierarchy
fixtures in `codec/jpeg/src/test/resources/`.
