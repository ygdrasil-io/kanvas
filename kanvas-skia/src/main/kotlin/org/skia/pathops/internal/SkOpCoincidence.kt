/*
 * Copyright 2026 The kanvas-skia authors.
 *
 * Forward declaration / skeleton for `SkOpCoincidence` (Phase D1.2.a).
 * The full coincidence class — coincident-segment-pair tracker +
 * mark / addOpenedCoin / addExpanded / mark / fixUp — ships in
 * D1.2.g.
 *
 * Note : distinct from [SkTCoincident] which is the per-perp-pair
 * helper used by [SkTSect] in the Bézier-clipping machinery.
 * `SkOpCoincidence` operates at the higher level of OpSegment pairs.
 */
package org.skia.pathops.internal

internal class SkOpCoincidence {
    // Body lands in D1.2.g.
}
