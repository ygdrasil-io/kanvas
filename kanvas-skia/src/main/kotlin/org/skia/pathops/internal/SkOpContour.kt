/*
 * Copyright 2026 The kanvas-skia authors.
 *
 * Forward declaration / skeleton for `SkOpContour` (Phase D1.2.a).
 * The full contour class — segment list management + global state +
 * resolveCoincidence + flow control — ships in D1.2.e.
 */
package org.skia.pathops.internal

internal class SkOpContour {
    // Body lands in D1.2.e.
}
