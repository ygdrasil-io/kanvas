package org.graphiks.kanvas.gpu.renderer.destination

/** Requirement to observe existing destination or backdrop pixels. */
class GPUDestinationReadRequirement

/** Accepted or refused destination-read plan. */
class GPUDestinationReadPlan

/** Destination-read strategy such as copy, intermediate, layer isolation, or refusal. */
class GPUDestinationReadStrategy

/** Conservative destination-read bounds. */
class GPUDestinationReadBounds

/** Binding contract for sampled destination snapshots. */
class GPUDestinationReadBinding

/** Ordering token for destination-read preparation and use. */
class GPUDestinationReadToken

/** Diagnostic emitted by destination-read planning. */
class GPUDestinationReadDiagnostic
