package org.graphiks.kanvas.gpu.renderer.clips

/** Captured clip stack descriptor. */
class GPUClipStackDescriptor

/** Plan for one effective clip element. */
class GPUClipElementPlan

/** Accepted or refused clip execution plan. */
class GPUClipPlan

/** Clip bounds and reduction plan. */
class GPUClipBoundsPlan

/** Scissor clip plan. */
class GPUClipScissorPlan

/** Analytic clip coverage plan. */
class GPUClipAnalyticPlan

/** Stencil producer-consumer clip plan. */
class GPUClipStencilPlan

/** Coverage-mask clip plan. */
class GPUClipMaskPlan

/** Registered clip-shader plan. */
class GPUClipShaderPlan

/** Ordering token for clip preparation and consumption. */
class GPUClipOrderingToken

/** Diagnostic emitted by clip planning. */
class GPUClipDiagnostic
