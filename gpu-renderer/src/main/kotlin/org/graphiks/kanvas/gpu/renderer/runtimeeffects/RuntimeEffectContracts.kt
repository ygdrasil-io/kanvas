package org.graphiks.kanvas.gpu.renderer.runtimeeffects

/** Registry of Kanvas-approved runtime effect descriptors. */
class GPURuntimeEffectRegistry

/** Descriptor for a registered runtime effect. */
class GPURuntimeEffectDescriptor

/** Stable runtime-effect descriptor identifier. */
class GPURuntimeEffectID

/** Version marker for a runtime-effect descriptor contract. */
class GPURuntimeEffectDescriptorVersion

/** Uniform schema accepted by a runtime-effect descriptor. */
class GPURuntimeEffectUniformSchema

/** Uniform block plan for a runtime-effect descriptor. */
class GPURuntimeEffectUniformBlockPlan

/** Child-slot contract for a runtime-effect descriptor. */
class GPURuntimeEffectChildSlotPlan

/** Resource binding contract for a runtime-effect descriptor. */
class GPURuntimeEffectResourcePlan

/** WGSL implementation plan for a registered runtime effect. */
class GPURuntimeEffectWGSLPlan

/** CPU oracle descriptor used for runtime-effect evidence. */
class GPURuntimeEffectCPUOracle

/** Route placement contract for a runtime effect. */
class GPURuntimeEffectRouteContract

/** Live-edit metadata and parameter constraints for a runtime effect. */
class GPURuntimeEffectLiveEditPlan

/** Per-recording usage set for runtime-effect descriptors. */
class GPURuntimeEffectUsageSet

/** Diagnostic emitted by runtime-effect lookup or validation. */
class GPURuntimeEffectDiagnostic
