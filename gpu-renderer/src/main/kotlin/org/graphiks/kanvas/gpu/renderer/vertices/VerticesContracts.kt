package org.graphiks.kanvas.gpu.renderer.vertices

/** Descriptor for DrawVertices or mesh-like input. */
class GPUVerticesDescriptor

/** Vertex attribute layout plan. */
class GPUVertexLayoutPlan

/** Per-vertex color handling plan. */
class GPUVertexColorPlan

/** Per-vertex texture coordinate handling plan. */
class GPUVertexTexCoordPlan

/** Primitive color blending plan for vertices routes. */
class GPUPrimitiveBlendPlan

/** Index buffer layout and upload plan. */
class GPUIndexBufferPlan

/** Vertex buffer layout and upload plan. */
class GPUVertexBufferPlan

/** Route selected for DrawVertices or mesh-like input. */
class GPUVerticesRoute

/** Render-step plan for vertices routes. */
class GPUVerticesRenderStepPlan

/** Future mesh-like 2D descriptor. */
class GPUMeshDescriptor

/** Diagnostic emitted by vertices planning. */
class GPUVerticesDiagnostic
