package org.graphiks.kanvas.gpu.renderer.pipelines

/** Render pipeline key for executable GPU render state. */
class GPURenderPipelineKey

/** Compute program key before compute pipeline creation. */
class GPUComputeProgramKey

/** Compute pipeline key for executable GPU compute state. */
class GPUComputePipelineKey

/** Diagnostic preimage used to explain pipeline keys. */
class GPUPipelineKeyPreimage

/** Compact cache key for pipeline lookup. */
class GPUPipelineCacheKey

/** Pipeline creation plan before resource-provider materialization. */
class GPUPipelineCreationPlan

/** Diagnostic emitted by pipeline key or creation planning. */
class GPUPipelineDiagnostic
