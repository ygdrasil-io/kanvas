# Task 1 — Breaker rulings after fix round 5

The five-round Task-1 loop is exhausted. No sixth Task-1 fix is authorized.
The final Sol gates identify real findings. They are not dismissed; each is
assigned to the smallest downstream task whose approved architecture can
remove the temporary legacy limitation.

## Load-bearing rulings carried into Task 2

1. **Canonical overlap export.** Before selecting any F32 representative,
   Task 2 must replace pairwise/inclusive overlap rematerialization with one
   canonical overlap component that aggregates exact incidences and uses strict
   interval interiors. No parameter scan may recover identities.

2. **Semantic IDs and ties.** Task 2 must assign unique deterministic IDs only
   after canonical multiset grouping. Raw edge/contour/source labels and IDs may
   not resolve geometric ties. Equal components aggregate incidences instead of
   competing by stable input order.

3. **Local Point witness anchor.** A `PointF64` may authorize only projected
   branches incident at the witness's exact source parameters, and the F32
   contact must equal the selected representative of that witness. A witness
   attached elsewhere on the same span grants no authority.

4. **Adjacent projected candidates.** Task 2's F32 broad phase must classify
   adjacent/backtracking sections unless exact source adjacency proves the
   projected relation is the same source event. No unconditional adjacent-pair
   skip is permitted.

5. **Behavioral pipeline tests.** Task 2 must delete or migrate the Task-1
   helpers that inspect/reconstruct graph objects. Required oracles go through
   public `PathOpsF32` and the full hybrid arrangement, asserting only geometry,
   membership, immutability, limits or stable public errors. Tests must fail if
   witness transport through the arrangement is neutralized.

6. **Budget prerequisites.** Every Task-2 index, sort, lookup, comparison,
   predicate and immutable-array construction uses the single candidate budget
   with deterministic preflight charges independent of comparator call count.
   Task 5 remains responsible for the complete end-to-end frontier, but Task 2
   may not add new unbudgeted or order/backend-dependent work.

## Rulings carried into Task 3

7. **Atomic claim endpoints.** `PathProjectedSpanClaimF64` must carry the exact
   endpoint identities needed to decide whether adjacent claims may share a
   boundary. Claims from different witnesses validate as one transaction before
   publishing topology.

8. **Collapsed incidence disposition.** `Drop` is permitted only when the
   whole selected contour is collapsed and exact double-area is at or below
   `2^-45`. A partially representable contour or a required local incidence is
   `Reject`.

## Rulings carried into Task 5

9. **Global complexity and budget frontier.** Replace any residual quadratic
   claim scan or per-overlap section rescan with indexed/sorted sweeps, debit
   deterministic worst-case bounds before work, and prove the same JVM/JS
   frontier immediately below and at success.

## Transition status

Task 1 established authoritative source spans/sections, removed compaction and
the raw split side channel, preserved seams, and carried provenance through the
legacy boundary. It is procedurally complete with nine parked, explicit
findings. Task 2 may proceed only with items 1–6 as prerequisites; otherwise the
hybrid arrangement would build on invalid witness authority.

