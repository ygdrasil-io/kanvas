package org.skia.tests

import org.skia.core.SkOpSegment

/**
 * C++ original:
 * ```cpp
 * class PathOpsSegmentTester {
 * public:
 *     static void DebugReset(SkOpSegment* segment) {
 *         segment->debugReset();
 *     }
 * }
 * ```
 */
public open class PathOpsSegmentTester {
  public companion object {
    /**
     * C++ original:
     * ```cpp
     * static void DebugReset(SkOpSegment* segment) {
     *         segment->debugReset();
     *     }
     * ```
     */
    public fun debugReset(segment: SkOpSegment?) {
      TODO("Implement debugReset")
    }
  }
}
